# 交接文档与skill资料归档验证

日期：2026-09-26。代码基线main `c7d03c0`。本轮仅整理交接和来源快照，用户随后要求重新检查，以下记录以本轮实际检查为准。

## 变更范围

- AGENTS和SESSION_HANDOFF改为当前状态入口，原阶段记录保留到两个历史归档；补回界面语言隔离、项目相对路径、凭据与图片缓存边界。
- 新增开发总清单、新session指南、技能候选与取舍及本地skill资料库；README更新继续项目入口，旧44项清单和需求历史加当前索引说明。
- 原始skill正文、脚本、参考及许可证按来源字节归档；不安装或执行，不改变生产rules、src、server、public、dist或原始用户参考。
- 新增本项目离线完整性工具skill-library/verify.mjs及原件换行保护规则。

## 独立复核发现与修正

1. 精简AGENTS时，界面语言不得修改来源／审核记录、运行路径不得绑定个人目录两项要求不够完整，已补回。
2. infographic的三张图是核心风格参考，不能当作非关键附件；补齐原件，同时保留其品牌素材不属代码MIT授权的说明。
3. content-repurposer和ecommerce-copywriter有随包MIT-0声明与来源卡片，区分“声明可见”“完整许可正文”“再分发已确认”，不笼统标无证据。
4. 补记录baoyu高权限宿主参数、infographic／long-form读取既有配置调用云服务、nano可变main自动下载覆盖的风险。没有运行这些脚本。
5. 修复历史AGENTS移动到docs后的README相对链接；候选索引采用可点击本地路径，本机来源不伪造网页地址。

## 本轮实际检查

| 检查 | 本轮结果 |
|---|---|
| `node skill-library/verify.mjs` | 29项来源、348个唯一原始文件、51,867,682字节，路径、完整清单、字节数与SHA-256全部通过；已有Git blob SHA-1与包hash记录一致性通过 |
| `node --check skill-library/verify.mjs` | 语法通过；工具不导入或执行来源代码 |
| 文档相对链接 | 13份维护／历史入口文档的256个本地链接全部可解析；上游原件故有链接不改写，已知缺口见manifest |
| Git忽略检查 | 348个manifest原件均未被Git忽略，不会因归档到仓库却被忽略而漏交 |
| 原件换行属性 | `sources/** -text -whitespace`生效，抽查正文与LICENSE均关闭转换；原件hash以当前字节为准 |
| 差异与范围 | `git diff --check`通过；生产src／server／rules／public／dist和docs/references原件无修改 |
| 独立审查 | 新session入口、当前能力、任务清单与资料完整度分别复核；发现项按上节修正 |

29项为9个GitHub技能、shuorenhua、既有用户s1a原件及18个Hub包。Hub208文件，GitHub／本机／既有参考140个唯一文件。infographic三张核心参考图已补回，仅Nano展示封面及x-mentor推广／系统附件省略。Hub许可分类为2项附全文、3项仅声明、13项未知，不把全部归档当作全部有许可。

此前预检发现验证报告链接未创建和历史README相对路径错误，均在最终检查前处理；Git的check-ignore退出码1表示没有忽略项，后续校验按该语义处理，没有将其误记为文件缺失。以上检查只证明交接和静态资料完整性，不新增产品运行证据。

## 未执行及不作的声明

文档整理及复核阶段未运行npm ci／build／产品测试、真实模型／图片生成、账号探测、服务启停、OAuth、平台上传／发帖。用户随后明确授权commit／push，提交前重新进行原件完整性和文档差异检查，Git操作结果以提交及远端状态为准。本轮无产品／public／构建输入变更，不为文档整理重建dist。历史整合测试的328/329及EBUSY仍保留为未解决问题，不因本轮链接／hash通过而变为产品全测通过。

快照完整性不证明逐行审查、脚本安全、生成质量、许可适用或外部服务可用；完整度针对实际包／选定子目录，遗漏的外部依赖、推广附件和链接问题以manifest为准。
