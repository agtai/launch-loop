# launch-loop 仓库迁移

目的：将已经完成的本地工作台、调研和最新需求迁入本仓库，让另一位执行者或新 Agent 会话可以独立启动、验证和继续开发。

本次沿用现有架构：React + TypeScript + Vite 前端，Node 内置 HTTP/SQLite 后端，Windows 启停脚本。保留兼容的数据标识和备份格式，不扩展新业务功能。

- [x] 复制代码、预构建页面、测试、许可、研究与需求资料。
- [x] 迁移用户提供的两份 Word 参考文件与项目 skill，并建立来源索引。
- [x] 配置本仓库 Git 身份，隔离运行数据、依赖、密钥和机器状态。
- [x] 编写可由新人复现的 README、会话交接及 Agent 入口文档。
- [x] 从新目录执行干净安装、生产构建、自动测试和实际启动。
- [x] 安全迁移当前本地记录，验证新地址可用和备份兼容。
- [x] 检查资料完整性、相对链接及待提交内容，形成迁移记录。

源文件和原始用户文档保留。机器运行状态不迁入 Git；账号信息只配置在本仓库 `.git/config`。远端发布与实际业务自动化不属于这次迁移的完成声明。

## 迁移结果

- 代码、初始化数据、预构建页面、测试、调研、最新需求、Word 参考、项目 skill 和原始手写笔记已归入本仓库。
- 项目包名和页面入口品牌改为 launch-loop / Launch Loop；保留 API 和备份的 `content-workbench-local` / 版本 1 标识，以兼容原有数据。
- 原本本机数据库及自动备份已迁到新目录的 `data/`，旧目录副本保留。源库与目标库的 49 条记录按内容摘要对比一致。
- Git 身份仅在当前仓库配置为 `hongxing <hongxing1227@gmail.com>`，没有更改全局配置。
- 原始 DOCX、skill 与笔记的校验值见 [参考资料校验表](references/checksums.sha256)。Word 和 skill 的源文件/迁移副本 SHA-256 对比一致。
- 运行数据、Node 二进制、node_modules、临时文件和 `.env` 被 Git 忽略。原始第三方许可保留原字节。
- README 提供安装 Node 后直接启动预构建页面、源码安装/构建/测试、日常保存、备份、端口冲突处理和新会话接续说明。

## 本次实际验证

环境：Windows x64，Node.js 22.23.2。验证日期：2026-09-23。

| 检查 | 结果 |
|---|---|
| 新仓库执行 `npm ci --no-audit --no-fund` | 通过，重新安装依赖；未复制旧 node_modules |
| 新仓库执行 `npm run build` | TypeScript 与 Vite 生产构建通过 |
| 新仓库执行 `npm test` | 9 项通过，0 失败 |
| 一键启动新仓库 | 通过，进程记录指向新目录，页面、5 个模块与 44 项任务可读取 |
| 浏览器打开本地页面 | 显示 Launch Loop、44 项工作、28 项复用资源 |
| 干净仓库文件副本启动 | 通过；副本没有 node_modules 和 runtime/node.exe，禁用原机器 LOCALAPPDATA 运行时查找，仅通过 PATH 提供 Node；HTTP 200，数据自动初始化为 5 个模块、44 项任务，随后按对应脚本停止 |
| 数据迁移 | 源库/目标库的 49 条记录逐项摘要一致 |
| 文档入口 | README、AGENTS、会话交接、参考索引和最新需求中的 45 个相对文件链接均可解析 |
| Git 内容检查 | 未纳入数据、依赖、Node 二进制或 .env；凭据特征检查未发现匹配；`git diff --cached --check` 通过 |
| 独立迁移审查 | 未发现阻断问题；已修正图像生成能力属于原 Agent 环境、并非克隆仓库自带的表述 |

测试不代表 macOS、Linux、Windows ARM 已验证，也不代表已连通 Claude / Codex、线上发布或反馈 API。后续实现与当前差距以 [会话交接](SESSION_HANDOFF.md) 为准。
