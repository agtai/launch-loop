# 本地工作台交付验证

## 2026-09-26 将唯一 X 提交 cherry-pick 到 main

用户明确授权将 `codex/x-content-publishing` 的唯一提交 `083bd70` 拣选到 `main`（原 HEAD `8bc5bb7`）。来源分支相对基线 `5942773` 的提交数为 1。本次解决共享文件冲突后，仍以一次 cherry-pick 提交交付；没有 push、实际模型／图片调用、账号连接或对外发布，也没有重启已有真实工作台。

冲突合并保留 main 的 LinkedIn `text-v2.0.0` 候选规则集成、母稿及冻结审核恢复、接受／拒绝／撤回／显式同步、图片正文 hash 绑定、OAuth 提示及未知发布结果核对；同时加入 X 独立规则、双格式、逐帖图片及发布流程。混合请求按平台分流，两个平台各自执行母稿流程。新增回归覆盖混合规则与图片路由、LinkedIn 平台／审核失败续接不改写 X，以及 X 改稿与 main 提案协议兼容。最终复核为共同事实变更记录追加来源平台，界面只向同平台另一语言提示同步，避免混合任务中误显示到 LinkedIn；旧记录仍按 LinkedIn 兼容。

- `npm ci`：通过，依赖审计 0 漏洞。日志 `tmp/x-cherry-ci.log`。
- `npm run build`：通过；最终分发产物为 `index-CgT4sn6z.js`、`index-DUvzGcPI.css`，保留现有大于 500 kB 的体积提示。首次及最终日志 `tmp/x-cherry-build.log`、`tmp/x-cherry-build-final.log`。
- `npm test`：首次 329 项全部通过，0 失败／取消／跳过，包含上述新增整合回归，日志 `tmp/x-cherry-tests.log`。来源平台提示修正后两次完整重跑均 328/329 通过；唯一失败均为 `launcher-proxy.test.mjs` 测试后清理临时 `runtime/node.exe` 的 Windows `EBUSY`，不是功能断言失败，日志 `tmp/x-cherry-tests-final.log`、`tmp/x-cherry-tests-recheck.log`。未改代码单独复测该文件后 3/3 通过，记录见 `tmp/x-cherry-launcher-recheck.log`；不把全套重跑写为全通过。本次未修改启动器或这份既有测试。
- LinkedIn 候选包契约：`node --test skill-preparation/linkedin-text/tests/contracts.test.mjs`，10/10 通过。日志 `tmp/x-cherry-candidate.log`。
- 浏览器：main 新构建、端口 4358、`tmp/x-cherry-browser/data/`，仅使用 `tests/fixtures/x-browser-server.mjs` 中明确标识的合成文本、1×1 测试图片及平台替身。完成 X 中英文普通帖＋三条串帖生成；观察逐帖加权字数、后台图片附入、AI 来源、选区改稿差异、接受后新 tmp 及受影响图片失效、撤回入口。检查 LinkedIn 缺配置提示和独立发布页面、X 英文发布页面与未保存稿不可发布；未发起发布。临时页面和该隔离服务已关闭，数据保留在 tmp。

官方计数语料保持来源文件及固定 hash，不为消除上游空白而改写。除该原样语料外差异空白检查通过。源码、构建和交接一起进入提交；tmp、运行数据、凭据及真实素材不入 Git。此前真实 X 文本与图片的证据继续位于来源工作区，本次不将其记作 main 的新线上验收。LinkedIn 图片质量／双语绑定及两平台真实 OAuth、媒体上传、线上发布仍按既有记录单列待验。

## 2026-09-26 独立 X 普通帖子与串帖适配完成

基线 `5942773`，分支 `codex/x-content-publishing`；不修改原工作区或另一 LinkedIn MVP 服务。完整本轮记录见 [X 验证报告](x/validation.md)，启动、看稿及共享文件合并注意见 [X 交接](x/handoff.md)。本节只适用于该独立分支，不覆盖下方 LinkedIn 历史。

X规则、官方加权字数、中英文双格式、工作台编辑／版本、后台真实配图及官方授权／多步发布适配已实现。`npm ci`、构建和207项全套隔离测试通过。9月24日浏览器合成资料已验证保存、版本1／2、逐帖预览、第二条失败后确认续发及图片重排；9月26日补验后台配图状态、取消、失败／重试和双语切换。跨模块复核发现的用户提前编辑与自动配图竞态已修复，保留旧LinkedIn和备份保护。

9月26日真实任务 `c7844518-6230-4c0d-9627-305e4903fa70` 完成中英文普通帖＋三条串帖，每语言审核一次，全部符合280加权字数。实际局改和接受到新tmp通过，其他对象与语言不变；四张1536×1024真实PNG完成后台生成及实际图片输入检查，改稿后的同图复核也通过。所有真实正文、审核、图片只在隔离 `tmp/x-completion-real/data/tmp/`，正式内容和X发布记录均0。工作台端口4356保留供看稿。用户已授权一个本地commit；未push、合并或实际发帖。真实X授权、媒体上传、线上发布／显示仍未验收，不以替身结果代替。

## 2026-09-25 本地提交前复核

用户授权将本轮成果 commit 至 `codex/mvp-integration`。`npm ci` 通过（依赖审计 0 漏洞），`npm run build` 通过，产物仍为 `index-DPo8zZ10.js` 与 `index-BKeF-FMP.css`；保留原有大于 500 kB 的打包体积提示。日志分别为 `tmp/mvp-commit-install.log`、`tmp/mvp-commit-build.log`。

首次 `npm test` 共 217 项：216 通过、1 失败。失败为 `launcher-proxy.test.mjs` 的隔离服务测试结束后清理临时 `runtime/node.exe` 时出现 EBUSY（hookFailed），日志 `tmp/mvp-commit-tests.log`。未改代码完整重跑后 217 项全部通过，0 失败／取消／跳过，日志 `tmp/mvp-commit-tests-recheck.log`。首次失败证据保留，不抹去或写成首次全通过。

检查只涉及安装、构建与隔离替身测试，没有重启 4348 真实服务、重新探测 Codex 或真实生成／OAuth／上传／发布。构建与回归通过不补足真实图片质量、双语绑定或 LinkedIn 外部验收证据。体验审计建议继续作为待办，本次没有实施。

## 2026-09-24 用户搁置网络后的本地收尾

用户要求不再纠结Codex连接，按网络问题暂时搁置；停止能力探测、真实重试与触发探测的服务重启。本地最后新增setup RPC安全分类（方法、数值码、有限类别，不记录原始message/data/headers）；runner仅映射受控界面提示，区分尚未开始生成。25项worker验证包含该新增用例；最终图像runner、受控宿主错误、素材边界、诊断继承及i18n共40项定向通过，0失败／取消／跳过，日志`tmp/mvp-image-local-final-tests.log`。之前质量brief和脱敏修正后216项全量也通过，日志`tmp/mvp-image-quality-all-tests.log`。最终构建日志`tmp/mvp-image-local-final-build.log`。

任务71d3真实出图成功但审图失败；任务1a6747b9在account/read内部错误-32603失败，没有开始图片生成，因此改进后的质量brief尚无通过证据。失败工件、原双语终稿全部保留，没有确认保存、LinkedIn上传／发布、commit或push。最终服务分类改动留待操作者重启时载入，避免本轮停止后又自动探测。

## 2026-09-24 图片终止通知及错误显示修复

本机公开schema验证终止`error`、`thread/closed`、图片结构化failure。worker新增准确身份匹配、willRetry=false立即结束、保留首次错误及安全分类；通知不会触发自动重试或额外出图。runner仅映射白名单为受控中英文提示，并通过ContentError贯穿任务／variant／asset状态；未知错误不输出provider原始细节。

35项定向测试、构建及216项全量通过，0失败／取消／跳过。日志`tmp/mvp-image-terminal-tests.log`、`tmp/mvp-image-terminal-build.log`、`tmp/mvp-image-terminal-all-tests.log`。Windows真实受控宿主的明确替身覆盖额度、授权、连接中断、thread关闭和未知错误，原稿与正式库保持不变；这些是本地边界验证，不代表真实账号遇到了这些错误。

真实重试`71d3b1cd-5798-4341-8a17-b6f219dec80d`由4348英文页面发起，原正文未改且原两条视觉诊断继承。实际图片结果及双语绑定以[MVP验收报告](MVP验收报告.md)最新节为准。

## 2026-09-24 续接延期配图项

用户恢复原延期工作。无模型能力探测最终代码实测11.915秒成功；原失败只能确认为30秒探测截止，不能据此认定账号缺失或某项网络故障。新增阶段诊断及显式重查（并发合并、取消与迟到结果保护），仍保持单次30秒、无自动重试。

`npm run build`通过，仍有原有JS体积提示；图片相关33项测试通过，全量210项通过（0失败／取消／跳过），日志分别为`tmp/mvp-image-resume-tests.log`及`tmp/mvp-resumed-all-tests.log`。4348浏览器验证启动检测成功、显式重查中禁用按钮、中英按钮翻译、重查成功恢复可用，且没有新增写作任务。真实质量复测与图文绑定结果以[MVP验收报告](MVP验收报告.md)最新节为准。

最后新增“传输超时不抹掉最近真实视觉发现”的边界修复，8项定向通过。第一次最后全量为210通过／1清理钩子失败：`launcher-proxy.test.mjs`测试临时`runtime/node.exe`在删除时EBUSY，日志`tmp/mvp-resumed-final-tests.log`；未改实现或降低断言，待服务启动操作结束后原样全量重跑，211项全部通过（0失败／取消／跳过），日志`tmp/mvp-resumed-final-recheck.log`。保留首次失败记录，不把它当作通过。

真实修正图片任务`e529f113-36f5-477f-9135-71f3dbaa4738`在420秒生成子阶段超时，未生成可收取图片，未开始视觉复测或中文图文绑定。最终`tmp/mvp-resumed-evidence.json`确认两份原稿正文逐字等于原修订工件、revision3、未确认、图片0，正式作品0、发布记录0、活跃任务0。重启后入口available；英文页面明确显示配图超时、正文仍可读，控制台无warn/error。原main仍干净且HEAD5942773；没有commit或push。

## 2026-09-24 LinkedIn MVP 整合轮

基线`5942773`，分支`codex/mvp-integration`，使用隔离dataDir和端口。完整实测、失败原因、修复与人工依赖见[MVP验收报告](MVP验收报告.md)。首次总回归158项通过；新增风险用例与修复后于`tmp/mvp-validation/2026-09-24T05-58-50-248Z/`执行npm ci、build、171项自动测试及10项候选检查，全通过；LinkedIn配置明确缺三项，独立标DEPENDENCY MISSING，OAuth与发布未验收。

最终全量203项通过（0失败／取消／跳过），日志`tmp/mvp-final-tests.log`。真实双语终稿、每稿一次有效审核、统一修订与一次选区改稿通过；中间超时、显式恢复及失败工件完整保留。后台官方入口已真实生成图片，自动视觉检查与人工看图发现歧义，拦截附图并保留正文。继承具体诊断的修复已通过回归，尚未做真实质量复测。用户要求即刻交付人工验收，剩余配图及外部OAuth／发布下次处理；不再新增真实调用。详细边界见本轮报告，以下保留历史记录。

## 2026-09-24 拣选 LinkedIn 文本技能候选到 main

用户授权将`11526bc`放到当前分支。本次基于`0dc93a2`拣选，唯一冲突为`docs/SESSION_HANDOFF.md`，已同时保留第三轮工作台状态和来源技能分支交接，注明候选尚未接入生产。`skill-preparation/linkedin-text/`的21个文件与来源提交完全一致；未修改生产src／server／rules、依赖或执行配置，未复制来源worktree的tmp。

本次实际执行`npm ci`（0漏洞）、`npm run build`、`npm test`（99项通过）及`node --test skill-preparation/linkedin-text/tests/contracts.test.mjs`（10项通过），均无失败。构建结果与当前已提交dist一致，仍只有既有主JS体积提示。`git diff --cached --check`通过，没有未解决冲突。没有运行真实模型、OAuth、配图或发布；本次仅把候选包带入当前分支并核对兼容性，不宣称它已经应用到工作台。未push。

## 2026-09-24 第三轮成果提交

用户追加授权将本轮成果形成一个本地commit并展示全部提交记录，未授权push或实际发帖。提交前核对Git、暂存范围和`git diff --check`，保留已完成的99项测试、收尾49项受影响测试及实际浏览器／模型证据；本次仅补充提交授权记录，不重复模型调用或无变化的功能测试。源码、说明资料、public和dist一并提交；tmp、真实data、依赖目录、运行时和凭据排除。其他分支已有的LinkedIn文本skill候选提交仅列示，不合并进本轮。

## 2026-09-24 第三轮：真实文本与改稿通过，配图／OAuth仍有缺项

基线`53b1dc9`，开始时Git干净。使用原任务内主Agent与三个Astra子Agent，没有新建独立用户session或扩编。未commit／push、未使用真实data测试、未替用户正式保存或发布。原始参考、规则版本text-v1.0.0和依赖lockfile保持不变。当前操作模式及未完成项见[第三轮结果](第三轮接入与真实验收.md)。

### 本轮命令与本地保护

`npm ci`通过（0漏洞）；TypeScript／Vite生产构建通过；`npm test` **99项通过，0失败、0跳过**。新增覆盖代理继承与父环境恢复、受控Windows进程代理环境、OAuth返回地址与编码scope、缺权限／过期状态。保留tmp泄漏、并发revision、确认增版、旧备份、事务回滚、恢复快照、epoch、任务取消／重启、发布版本绑定与防重保护。自动测试使用隔离数据和明确替身，不调用真实模型或平台。public指南／需求／授权说明及dist同步，构建主JS超过500kB的体积提示仍存在，不影响构建成功。

网络排查核对服务、Node和受控CLI的进程代理；不修改全局设置、不关闭TLS、不读取认证token。无凭据小请求显示直连受阻、现有系统代理可到达外部TLS站点；HTTP403不代表模型账号认证失败。生产受控执行器最小真实调用22.4秒完成，同document schema精简材料调用50.5秒完成。大输入真实UI任务仍超时，因缺具体网络错误证据，不证明所有超时都由网络引起。随后使用真实节选并明确只执行当前阶段，完成完整链路；这两项变化不支持归因于单一原因。

### 实际工作台任务（全部未确认保存）

浏览器使用独立服务`tmp/round3-browser`、端口4336。本轮通过真实文件上传选择`product-materials-excerpt.md`（2699字节），来自system1-agents固定commit`0f0b4c9eca03127875cfdf0d941fbd22c16478a4`的README及docs/skills.md。写作目的为解释有限选项决策与工单路由，面向智能体开发者；第三方视角、简明文字，不假称实测性能或第一手体验。上传字节、来源和hash进入tmp，不仅传文件名。

| 任务ID | 实际结果 |
|---|---|
| `9e4761fb-55c9-4961-83c3-1060b1a5d90b` | 较大材料首个中文初稿300秒超时；无完成事件、无有效正文，后续阶段未执行；保留失败，不复用输出 |
| `7a48bd1f-bdb3-4c44-9181-c6afda33412b` | 中英文各初稿→一次审核→修订共6次真实调用，均本次turn.completed、退出0且输出有效；实际Astra Xhigh、text-v1.0.0 |
| `db052dc9-182f-434e-b595-6ceb04fc3b45` | 中文选区真实改稿完成；UI接受前原稿不变，接受后派生tmp，选区外及其他段落不变；手动修改的第4段保持 |
| `e0251337-f22c-4ce2-8b5c-d4917a11db97` | 调用运行时手动修改源稿，模型实际完成后因revision变化拒绝迟到结果，未生成可接受提案、未覆盖新编辑 |
| `569b7c08-65b6-4406-ac1f-5322d5f5561e` | 新请求真实完成并有独立输出；原拟测试取消但结果先返回，未将其记为取消成功，未接受该提案 |
| `be48aa46-abc7-4a9a-bc54-5b426ecb26e0` | UI新请求后立即取消；最终cancelled，受控结果code125／cancelled=true、无完成事件和有效正文，未形成提案。只证明本次执行启动／取消路径，不声称外部模型已开始推理 |

两语言自动审核均针对实际初稿引用具体句子，指出工程接入责任不能写成来源已证实的实现；最终稿据此区分资料事实与第三方判断／接入要求。没有把审核清单本身当审核结果，没有无限循环或新增第二次审核。保留来源快照、未实测性能、平台实际计数／提交和配图未连接等未解决项。英文原审核中“修订属后续步骤”的阶段性提示与最终稿“待用户确认”一起保留为历史审核原文，不代表修订没执行。用户仍应核对稿件；本轮未访问产品运行环境或宣称外部事实检测工具已运行。

初始中文tmp`5003e3db-55d5-484b-a3d2-bd3c4c5b0a57`；英文tmp`76e3b563-985f-447d-b77a-5e36a9d62653`；接受局改后的tmp`bd7c57b1-dbad-4d7e-a82c-10a0324ae623`。接受不执行confirm，旧tmp继续保留。实际库与备份正式内容均0、发布记录0；逐段核对真实tmp正文没有进入正式备份。真实稿件未保存，所以本轮不可变正式版本／恢复保护的证据来自自动测试，不冒充用户已批准生产稿。

浏览器还验证：完成的中文稿可在英文仍执行时查看／编辑；最终中英文独立切换；接受后的派生tmp仍显示持久化未解决问题；取消、失败及自动配图未连接均如实显示。最终发布页没有任何可发布正式作品，准备发布按钮禁用。OAuth返回标记仅选择发布页，不能伪造连接状态；无效callback显示固定双语失败页并可返回工作台。合法303、scope解析与失效路径使用本地协议测试，**不算真实OAuth成功**。

收尾审查修复了查询帖子失败后页面未刷新已失效连接的问题。另起隔离合成账号服务，通过实际浏览器点击核对按钮，替身返回401后界面立即显示“尚不可发布”和重新连接提示，原记录仍unknown，不重发；没有连接真实平台。补跑发布／语言相关49项测试通过并重新构建。实际文本服务使用对应停止／启动脚本重启后，全部任务状态、输出hash和7份tmp与重启前相同，没有自动重跑；证据为`tmp/round3-validation/restart-evidence.json`。真实双语原修订稿包含标题的长度为中文360、英文767个UTF-16字符。

### 外部能力与保留证据

- 后台自动配图：**未完成**。官方Codex能力探测与只读沙箱小探针完成，没有调用图片模型。官方缓存位置与项目tmp要求尚需操作者明确选择；API替代方案尚未配置且单独计费。未用会话图片、提示词、上传图或占位图代替。
- LinkedIn：**未真实授权或发布**。进程、用户、机器层未配置应用ID／secret／HTTPS回调；缺产品权限和操作者OAuth。无实际账号或scope证据。配置指南已提供必要字段、callback-only代理范例与安全输入方式，范例未部署，不能视为HTTPS验证。
- 本轮实际图片上传到平台、帖子提交、真实ID／URL和最终可见状态均未执行。发布仍需具体账号、用户保存版本及配图确认；没有发帖授权。

私有运行证据保留在`tmp/round3-validation/audit.json`（任务阶段事件、输出hash、暂存／备份分离）、`tmp/network-20260924/`（短请求与schema调用）、`tmp/round3-image/`（配图入口探测）、`tmp/launcher-system-proxy-manual/`（实际启动器代理继承）和隔离服务的`data/tmp/`。Git仅记录证据索引、任务ID与结论，不复制正文、提示或敏感诊断。克隆不会包含这些tmp；交接时由操作者明确转交临时内容并重新登录。

最终构建脚本为`index-2Pj2sIIP.js`，最终浏览器确认已加载该版本。`git diff --check`通过；静态副本与来源一致。本轮保留4336隔离看稿服务，临时数据不清理；合成401浏览器测试服务已单独结束，其证据在`tmp/round3-reconcile-u3HEau/browser-result.json`。

## 2026-09-24 工作台中英文界面

基线为第二阶段提交 `4b42ab9`；用户随后授权全 UI 中英文切换。本轮新增显示层语言上下文、日期格式、1,667条合并后的英文词条和顶部／弹窗内语言开关。覆盖创作、发布、任务、资料、历史模块、架构、使用说明、备份恢复提示与受控后端错误。未新增依赖或修改后端存储协议；原始任务／资料 JSON、data-seed、references 保持不变。完成后用户于2026-09-24追加授权本地commit，未授权push或对外发布。

提交前核对源码、词典、测试、文档及public／dist构建结果，补充上述授权记录；没有新增功能修改，不重复已完成的76项测试与浏览器验证。临时资料、实际data、依赖目录及密钥均排除在提交外。

- `npm ci`：通过，既有 lockfile 安装，0漏洞。
- `npm run build`：TypeScript与Vite生产构建通过；public指南和dist同步。完整英文资料词典增加了前端体积，Vite提示主JS超过500kB；这是体积建议，不是构建失败，本地离线界面可正常使用。
- `npm test`：**76项通过，0失败、0跳过**。保留第二阶段72项保护，新增4项检查覆盖所有内置任务／资料／种子描述、原数据不变、模板参数与文件名、嵌套错误翻译、UI文字遗漏、语言偏好与日期格式。普通自动测试未调用真实模型或LinkedIn。
- `git diff --check`：通过。没有修改依赖锁文件、实际data、原始参考或源资料。

浏览器使用 `tmp/i18n-browser` 的独立数据库与服务，端口4334，未使用真实data。实际验证：中文进入后填写资料／目的／读者、选择作者身份，切换English后全部输入和中英文稿件选项保留；各导航页、任务全文、资料详情及产品比较均显示英文；发布页明确显示英文未连接／缺配置与其他平台待适配，未授权或执行发布。

任务与模块弹窗中编辑中文记录，往返切换后未保存输入保留；在英文界面明确保存至隔离数据库后，API断言负责人／记录原文、中文状态枚举及未修改的种子字段完全不变。界面内置默认模板译文只用于显示。右侧使用明确标注为“UI-only fixture”的合成稿件测试手工改稿与切换；标题／正文保留原文并自动暂存，正式内容列表及备份仍为0，不冒充模型输出。

刷新后英文偏好保持，另一标签切回中文后原标签同步；document.lang和标题随之更新。桌面以及390×844视窗检查通过，页面和弹窗无整体横向溢出，语言／关闭控件可见；资料导航的内部水平滚动保留。浏览器error日志为空。测试标签收尾关闭，视窗覆盖恢复；测试服务按对应停止脚本关闭，隔离数据和证据保留。

验证必填错误提示时，测试浏览器的清空操作未按预期改变字段，误点击生成，形成隔离任务 `043b53df-cb6d-4739-ad98-959a00e22412`。发现后立即通过取消按钮停止，最终后端状态为cancelled、受控模型进程已终止，两语言暂存均无文档，未执行审核／修订或正式保存。该意外调用不算真实生成验收，未继续重试；随后核对取消结果的双语提示。原第二阶段真实生成网络问题、后台自动配图及LinkedIn授权缺项未因此通过。

本地证据保存在 `tmp/i18n-validation`：`data-preservation.json`为初次任务／模块保存后检查（当时尚未触发生成）；`final-preservation.json`为最终tmp／正式库／取消任务核对，`cancelled-job.json`保留取消状态。临时证据不进入Git或公开资料。

## 2026-09-24 第二阶段成果提交

用户追加授权本地commit，未授权push或对外发布。提交前核对Git，变更均为上一轮第二阶段源码、测试、文档及同步的public／dist；补充提交授权记录，没有新增功能修改。git diff --check通过。本次不重复调用模型或运行已完成的功能测试，沿用下方2026-09-23的72项通过和构建证据；真实生成的网络阻塞、配图入口和LinkedIn授权缺项状态不变。tmp、真实data、运行时、依赖目录及密钥均不提交。

## 2026-09-23 第二阶段：实现与本地验证完成，真实链路待网络／权限验收

基线cd81286。范围见[第二阶段接口与分工](第二阶段接口与分工.md)，接口见[执行与发布说明](第二阶段执行与发布接口.md)。使用当前任务内主Agent＋三个Astra子Agent，无独立用户session；未commit、push、真实发帖或使用真实data/测试。未修改原始references、data-seed或依赖文件。

### 本轮实际命令

- npm ci：通过，按既有lockfile安装依赖。
- npm run build：TypeScript和Vite 8.0.16生产构建通过，最终脚本index-jtdD_nDR.js；public指南／需求副本与dist同步。
- npm test：最终 **72项通过，0失败、0跳过**，包含原第一阶段30项保护、受控执行与来源／审核测试、发布26项专项、Windows数据目录租约崩溃恢复与3项新增整合测试。普通测试全部使用临时目录；模型和LinkedIn网络接口是明确替身，不作外部能力证据。
- git diff --check：通过；仅提示共享类型文件的CRLF→LF规范化，没有空白错误。

### 行为与保护验证

自动测试覆盖：实读上传字节与规则identity；每语言初稿→一次具体引用审核→修订；未解决问题保留；未确认正文／素材／提示／审核不进SQLite或备份；独立确认、不可变旧版、revision冲突；局部修改只替换准确选区、源稿变动阻止迟到结果、接受建议仍只tmp；请求幂等、失败、取消、超时及重启interrupted。Windows使用真实受控Node测试进程验证stdin字面量、仅本任务进程树终止、父进程死亡不遗留子孙进程，未调用模型。

发布替身测试覆盖：OAuth随机／过期／一次性state及UTF-8比较、实际scope、明确正式版本／账号／素材绑定、图片上传顺序、拒绝不可信上传域、明确确认、重复点击和新预览hash防重、401／403失权、网络未知及缺ID不报成功、不自动重发、已知ID只读核对、重启unknown、旧新备份合并不擦除发布事实、防重与容量预留、事务及恢复前快照、发布中拒绝恢复、同数据目录双服务拒绝。测试中的平台ID、账号和凭据均为合成值。

### 浏览器实际验证（独立服务4331）

服务、SQLite、素材、快照均位于tmp/stage2-browser，未使用根data。真实资料是第一阶段保留的system1-agents文档快照；实际上传TXT/MD内容与第一阶段已有PNG概念图，非占位图。上传图只证明上传与预览路径，不能算本轮自动配图。

实际界面触发两次双语生成，看到任务状态及超时错误；重载可恢复任务记录。核对发布页明确列出缺少的应用配置，未保存稿件不在正式选择列表；X显示待适配，反馈页留空，任务／资料／历史入口保留。

因网络阻塞，后续UI验收从tmp/round1-writing导入第一阶段实际中英文试稿到同一隔离tmp，名称和postingNote明确“已有真实试稿／非第二阶段CLI产出”。实际验证右侧中文／英文切换、PNG可读显示、段落手工修改及tmp自动暂存、选中文字与修改意见控件。**未点击模型改稿提交，也未把控件检查当改稿成功。** 备份初始正式作品为0；手工修改不自动进入正式库。

在隔离库通过按钮确认中文v1，开始新修订再确认v2；旧版正文和图片保持可读且只读。后端断言两版段落不变、仅新增标题、英文仍排除在正式备份、发布记录为0。发布页列出中文正式作品与明确历史版本，正文和图片来自同版；账号未配置使发布预览确认入口禁用，没有调用execute。

通过页面导入刚导出的隔离备份并确认恢复，生成恢复前快照；英文tmp仍可读、显示过期且不能确认，点击显式派生后恢复可编辑，正文保留。浏览器控制台未发现error。UI复审补强恢复时旧暂存响应与切页后迟到预览的防护；历史查看版本标签已按实际选中版本修正。

本地证据：tmp/stage2-validation/product-materials.md、imported-trials.json、ui-storage-evidence.json、ui-backup.json；这些含试稿／备份的证据不入Git。隔离库内“确认保存”是测试动作，不代表用户已批准生产内容，更不授权发布。最终构建中已复查历史版本标签正确显示“正在查看版本1”；收尾通过对应停止脚本停服并关闭测试页面，保留数据与诊断。public与dist中指南／需求副本逐字相等检查通过。

### 真实模型：未通过，网络证据与边界

- 14:02:18 UTC从工作台触发job acf5eab2-8561-4c7b-97c5-67c1f3a95b28。模型目录超时，WebSocket多次重连后转HTTP，300秒结束，没有正文或有效完成事件。
- 修复CLI重连警告的错误分类和超时元数据，按官方配置设置本次调用HTTPS provider。无模型的debug models --bundled解析通过，识别9个模型含Astra；这不证明联网成功。
- 14:45:03 UTC从工作台新请求触发job d9920beb-c3e7-454f-baee-e140ab86e466。没有WebSocket重试，但模型目录／分析请求仍超时，300秒结束，没有正文。两个job均在首个中文初稿失败，中文审核／修订、英文三阶段未执行，真实Agent局部修改亦未执行。
- 用户指出可能为当前网络问题。随后普通Node和同一Job Object宿主对ChatGPT、Learn、Microsoft的无凭据HTTPS请求均连接失败；独立Windows curl对ChatGPT和Microsoft也在TCP/TLS尚未建立时超时、HTTP 000。启动器未清除代理环境，宿主继承哨兵成功；Job Object只设置进程退出约束，无网络限制。证据指向当前本机直接外网连接受限，不能确定ISP／VPN／防火墙具体原因，不能归因于图片或Xhigh。已停止继续模型重试。
- 只读Windows代理配置查询被自动审批审查拒绝，仅返回blocked by policy；未绕过。诊断改用不读配置、不含凭据的公开HTTPS请求完成，记录tmp/stage2-network-diagnostic.json。没有读取或提取会话token。

### 仍待验收

网络恢复后，用真实UI重新验证中英文独立初稿、一次审核、修订和局部Agent改稿，再核对实际正文、来源、未解决问题及迟到结果保护。当前受控适配代码与测试完成，不能宣布真实文本链路已打通。

自动配图后端尚无已授权官方／受支持入口，显示未连接；会话imagegen能力不作后台证据。LinkedIn缺实际应用配置、产品权限、HTTPS回调及用户OAuth，真实图片上传、帖子创建、平台ID／URL和实际显示未验收。真实发帖必须另行确认具体账号和版本，本轮没有该授权。完整MVP仍未打通。

## 2026-09-23 第一轮有限开发：最终验证

授权范围见 [第一轮实施范围](第一轮实施范围.md)。开始时 Git 干净，未完成草稿已包含在 checkpoint `1d74cb0`；不是新出现的未提交用户修改。开发完成后用户追加授权 commit 本轮成果，未授权 push 或发布；提交不包含 tmp、真实 data/ 或密钥。

### 命令与环境

环境：Windows x64，Node.js 22.23.2。

| 检查 | 本轮实际结果 |
|---|---|
| `npm.cmd ci` | 通过。初次安装发现 Vite 8.0.13 高危告警；最小升级至 8.0.16 后重新 ci，通过且 0 vulnerabilities |
| `npm.cmd run build` | 基线修复后通过；最终整合再次通过 TypeScript 和 Vite 8.0.16 构建，1797 modules，dist 已同步 |
| `npm.cmd test` | **30 项通过，0 失败、0 跳过**，约21.0秒；全部使用隔离临时数据目录 |
| `node --check scripts/preflight/codex-text.mjs` | 通过；另通过缺失 CLI 的失败路径测试，返回退出码1、ENOENT且不报告生成成功。未重复消耗账号额度执行 |
| `git diff --check` | 通过；只有 Windows 换行规范提示，无空白错误 |
| 原始参考校验 | 两份DOCX、s1a-launch-kit/SKILL.md、original-notes.jpg与原checksum全部一致 |
| 文档与类型一致性 | 13份本轮Markdown文件无失效相对链接；接口文档类型与src类型一致；public与dist两份下载文档字节一致 |
| 临时产物边界 | `git check-ignore`确认真实试稿和生成PNG在tmp被忽略；未放入public、dist或正式内容库 |

### 自动测试覆盖

- 13项内容测试：旧records数据库增量迁移及重启；tmp正文、初稿、审核、提示词和二进制不泄漏主DB/WAL或备份；素材实际字节晋升；并发暂存写与确认；新版本保留旧版；平台语言与多格式关系；仅上传文件作为必填资料；旧备份保留正式内容并使旧tmp失效；新备份往返全部版本和素材；篡改／事务异常回滚；URL、来源、路径、跨源和执行元数据校验；近16MiB容量；安全整数与集合边界；近整数上限的导入revision恢复后仍可修订和确认且旧页面失效；持锁进程异常退出后恢复写入。
- 7项规则测试：确定性、选项排序去重、5平台×2语言份数、未适配不回退、未定默认状态、畸形输入、多格式合并、CTA不必填、术语冲突和规则hash变化。
- 1项接口联合测试：实际规则解析结果写入中英tmp，单独确认中文、英文保留tmp且不入备份，元数据一致，保存不创建发布批准字段。
- 原9项回归：模块／任务持久化、revision、恢复与快照、超过2MiB备份、校验／跨源／静态路径，以及Windows启动器身份、重复启动和占用端口保护。

异常回滚测试有意注入 `synthetic asset failure` 与 `synthetic restore failure` 两条SQL错误，测试断言预期500且数据完整回滚；它们不是未处理的测试失败。Node内置SQLite仍输出experimental提示，不表示测试失败。

整合复核额外修复：恢复只使用本机revision序列，并跳过恰好相同的导入token，导入极大revision不会抬升本机计数器或授权旧页面写入；tmp互斥改用SQLite事务锁，消除崩溃后遗留文件锁；旧模块／任务写入也维持完整备份容量，快照紧凑JSON避免超出恢复上限；纯文件资料在创建tmp时可同时上传并重映射引用，无需虚构文本才能开始。

### 浏览器与构建产物

通过本机浏览器工具验收 `tmp/round1-browser/` 隔离副本，使用端口4327及其自己的数据目录，未启动或停止真实工作区服务：

- 页面正常加载旧五步工作台，没有缺失组件、虚构创作入口或内容生成成功状态。
- 在隔离库填写模块负责人、保存、刷新后值仍存在；随后用备份恢复清除该测试修改。
- 新版备份弹窗显示正式内容集合和素材恢复、旧集合进入快照、tmp保留及恢复后需派生的说明；实际截图确认文字、按钮和布局可读。
- 旧版备份弹窗明确保留现有正式内容；新旧恢复均看到“备份已恢复，恢复前的数据已另存快照”，临时目录内产生恢复前快照。
- 浏览器console error列表为空。最初浏览器工具枚举连接失败，重试直接打开本地页后恢复，未作为通过证据使用失败调用。
- 验收后已关闭临时页，并使用该隔离副本的Stop-Workbench.ps1停服，临时数据保留。

前端改动仅为可构建基线和现有备份预览说明。完整三步导航、右侧阅读／编辑和生产执行界面未实现。`public/docs/guide.md`及`requirements.md`已同步当前资料，dist为本轮最终构建。

### 真实外部能力与试稿

[接入预检](第一轮接入预检.md)记录：现有ChatGPT登录的Codex CLI两次真实双语短文本调用成功；第二次发生request timeout重试后回退HTTP，不能宣称稳定生产接入。未发现本机Claude CLI。LinkedIn只核查官方路径及权限，没有连接账号或试发。

[文本规则整理](第一轮文本规则整理.md)记录：基于真实用户参考和固定产品commit完成中英LinkedIn普通动态初稿、各一次内部审核和修订；中文540、英文1068字符，仅tmp，未替作者确认。版本化规则及26条来源记录可供后续执行器直接使用；其他四个平台、Pulse和串帖仍待适配。

本会话实际调用内置imagegen一次成功，PNG为1536×1024、2021780字节，SHA-256 `b38d1d122f411c0e65b2f9942c518b3f0fb0ee64908c6c0a9cac86b00cac758f`。已目视检查概念关系与英文标签；项目文件、最终提示词和验证记录位于`tmp/round1-assets/`，未加入正式素材库。工具自身默认生成缓存不代表工作台正式保存。

这些证据分别证明CLI或会话工具能力、规则试稿和存储实现；**不证明工作台已接通生产级Agent、配图后端或完整MVP**。没有执行产品基准、外部文风检测、DOCX页面验收、发布或反馈采集。后端程序化配图入口、生产文本执行可靠性、LinkedIn应用／账号OAuth授权及发布结果仍是后续缺项。

README本轮按release-sync处理本地应用能力和备份语义：保留现有启动路径，纠正“备份只有模块任务”和“下一阶段采集反馈”的旧说法；未增加发布、跨平台或生产就绪承诺。

## 2026-09-23 当前修改提交前检查

用户要求将需求讨论记录和已暂停的代码草稿合为一个提交并推送。本次只保留当前工作状态，不恢复功能开发；下方原交付版本的通过记录不代表当前源码可以构建。

- `npm.cmd ci --no-audit --no-fund`：通过。
- `npm.cmd test`：9 项通过，0 失败；测试使用临时数据目录。
- `npm.cmd run build`：失败。`src/App.tsx:7` 引用尚不存在的 `./ContentWorkspace`，TypeScript 返回 TS2307；Vite 构建没有执行，`dist/` 保持原交付内容。
- `src/App.tsx` 和 `src/content-types.ts` 作为未完成草稿保留，不宣称已实现内容工作台；现有后端与启动器测试通过也不能证明这些草稿可用。
- 未做新界面的浏览器验证，未调用生成或发布服务，未修改真实运营数据。

## 原交付版本记录

日期：2026-09-23。环境：Windows x64；Node.js 22.23.2；浏览器本地地址 `http://127.0.0.1:4318/`。

## 已完成验证

- `npm run build`：TypeScript 检查和 Vite 生产构建通过。
- `node --test tests/*.test.mjs`：9 项通过，0 失败。
- 后端测试覆盖模块/任务保存后重启、并发版本冲突、无效恢复不改数据、合法恢复及恢复前快照、大于 2 MiB 的中文内容备份往返、字段和 URL 校验、跨来源写入拒绝、静态文件路径限制与 CSP。
- Windows 启动测试覆盖重复启动、真实重启持久化、拒绝停止无关 PID、文件夹搬移和 PID 复用后安全重新启动、占用端口不误停其他程序。
- 实际从交付目录的一键脚本启动并停止，再重新启动成功；使用随包附带的 Node 运行时。
- 浏览器中实测：编辑模块后保存；编辑任务负责人和状态后刷新仍保留；复制任务包包含输入、步骤、验收、依赖及复用说明；关闭未保存的编辑窗口会显示继续编辑/放弃修改；竞品检索与详情、架构页、使用说明正常显示。
- 当前浏览器宽度 592 像素下，页面正文没有超出视口；导航和宽表格允许内部横向滚动。
- 内容独立复核：44 个任务 ID 唯一、依赖存在，清单与页面一致；28 项资源均有复用对象、方法、理由；16 个产品均有官方来源及证据边界。复核发现的技能原文链接缺失和附录路径已补正。
- 界面测试使用的负责人及状态已恢复为空/待开始，未留下虚构运营成果。

## 修正与未覆盖范围

本次修正了完整备份可能超过恢复接口容量、失效进程记录阻止重新启动、关闭编辑器直接丢弃修改，以及冲突后缺少保留草稿并读取最新版本入口的问题。恢复上限现为 16 MiB；普通写入仍为 2 MiB。

“下载草稿并载入最新版本”入口已实现并通过构建；并发冲突由后端自动测试验证。本次未逐个做所有浏览器版本与系统版本的兼容测试。便携包适用 Windows x64，不宣称已在 macOS、Linux 或 Windows ARM 上验证。

供应商研究仅核验官方资料，没有登录商业产品试用或连接社媒账号。资料与文案样例为未发布草稿，没有把平台曝光、真实反馈或产品基准执行列为已完成。

本地工作台是可持久化的运营台账、方法库与交接入口；目前不自动调用模型、不自动发布、不抓取评论，也不提供多台电脑实时协同。
