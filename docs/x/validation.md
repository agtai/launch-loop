# X 本轮验证

日期：2026-09-24／26。基线 `5942773`；隔离分支 `codex/x-content-publishing`。用户9月26日要求完成 X 并形成一个本地 commit；未 push、合并或向 X 发送真实内容。真实稿件与图片仅在隔离 tmp；合成资料用于正式保存、版本与替身发布验证。

## 自动测试

9月26日实际执行 `npm ci`（0漏洞）、`npm run build`、`npm test`。最终全套 **207项通过，0失败／跳过**；最后仅补了局改预览的X格式标题与一条受控提示的显示翻译，重新 build 与 i18n 4项检查通过。日志位于被忽略的 `tmp/x-resume-ci.txt`、`tmp/x-completion-build-final.txt`、`tmp/x-completion-test-final.txt`、`tmp/x-completion-i18n-final.txt`。旧 twitter-text 依赖链有 core-js 弃用提示；构建仍有主 JS 大于 500 kB 的体积提示。没有以删除原有 LinkedIn 测试解决冲突。109个本地文档链接检查无缺失。差异空白检查仅官方原样计数样例的第651行空格和末尾空行有告警，保留上游数据及来源hash；排除该上游样例后检查通过。

覆盖官方加权计数样例与中英／URL／组合字符／emoji／编号边界、双格式同包、稳定逐帖 ID、一次审核、局改范围、图片关联、版本保存／恢复、OAuth／权限、完整计划绑定、逐帖执行、未知结果、重启、取消、部分失败续发和重复确认。平台调用与模型调用使用明确替身；没有拿这些测试证明真实 OAuth、媒体上传或线上显示。

自动检查及交叉复核发现后已修复：元数据变化、并行上传或把已发前缀移到计划后段可能绕过防重；恢复备份漏掉素材可选 AI 标记；正文变化未保留已知 AI 来源；重排中间帖子丢失首图；相同 block ID 的图片输入状态混用；保存空白子帖；续发预览未同步原版本；异步刷新乱序及返回发布页漏刷新。相应自动或浏览器验证见下。

续作另外修复：母稿混入目标语言规则、图片说明更新越过当前对象、图片面板与文档组件重复key导致切语言留下重复面板、OAuth原始回调地址校验、媒体等待中预览失效，以及双语第一份已被用户改成无图后仍启动默认配图的竞态。最后一项已先复现失败，再用最终文本冻结revision修复；未编辑的另一语言仍正常完成两个对象的图片任务。图片、文本、发布 Agent 相互只读复核后，无未解决的确定性阻塞问题。

## 实际浏览器

9月24日使用 Codex 内置浏览器访问本轮 4346／4347 服务。4347 使用合成资料、合成模型及合成 X 账号，界面和真实 HTTP／存储代码不替身。

- 从创作表单选择 X、中英文、普通帖子＋串帖，显示两份平台语言稿，每份包含两个内容对象。
- 英文普通帖编辑到 281 加权字，显示超限并保留原输入，没有截断；恢复正文后继续操作。
- 中文／English 界面切换保留实际英文正文。刷新页面保留 tmp、正式内容与发布结果。
- 合成英文确认保存为版本 1，发布页只显示正式版本；中文未保存稿不会进入选择。发布时明确选 thread，未自动发布普通帖或另一语言。
- 合成串帖第 2 条返回 422 后，第一条保留已发布、第二条失败、第三条待处理，整条显示部分成功。使用原记录重新核对并确认，成功 IDs 为 5001、5003、5004，首条未重发；这些编号完全是测试值。
- 发布预览显示准确账号、作品名称、版本日期、逐帖文本／计数及已发布前缀。三条完成才显示整条完成，实际链接结构为 `/i/web/status/…`，未访问这些测试链接。
- 从版本 1 开始新修订，经浏览器文件选择器上传明确 1×1 合成 PNG；首帖显示真实文件名与尺寸，其余帖子不能挂图。填写描述并确认目检后显示用户已检查。
- 重排第二／第三条时首图仍保留，状态改为需要重新检查；首帖带图时不能直接移走。恢复顺序保存合成版本 2，切回版本 1 没有新图，旧正文不变。
- 实际截图检查发布区两栏和逐帖结果，无明显遮挡或溢出。此截图只验证该视口布局，不宣称所有移动设备已验收。

9月26日使用4356真实运行目录及4357明确合成环境补验：

- 工作台实际发起新的中英文普通帖＋三条串帖；文本完成后后台自动分别生成四张图，页面自动读取当前tmp的新图片、尺寸和检查结果。
- 中文→英文→中文后只保留当前稿的一组图片面板；界面切换不改实际正文。新图片任务状态和受控错误已补齐英文，实际审核发现／图片观察保留来源语言。
- 合成环境明确选择普通帖“无图片”后，仅更新串帖说明，普通帖保持无图；已知AI来源不可取消。
- 图片任务取消显示取消结果；两次过快的合成任务在点取消前已结束，未计为取消成功，第三次明确取消成功。失败案例保留已编辑正文和既有图片，显式重试后新图正确进入当前对象。1×1合成图只验证此交互，不作为素材质量证明。
- 在真实中文普通帖发起局部改稿，提案接受前原稿不变；接受后产生新tmp，普通帖最后一句改变，串帖逐帖文本／ID与英文稿保持原样。普通帖旧图正确变为需要检查，串帖图仍保留检查通过。随后对改稿的原图执行真实视觉复核，通过后恢复关联。
- 重启对应4356服务后，真实稿、四张图片及任务结果仍在，未自动重复生成。图片面板及真实原图完成截图／目视检查；没有代用户点“确认保存”或真实发布。

## 真实模型

9月24日任务 `672b7d9b-fd55-45ac-a987-ed867fe6af27` 按用户“停止”要求取消，不计为完成。9月26日新的真实任务 `c7844518-6230-4c0d-9627-305e4903fa70` 从4356工作台表单发起，07:28:14至07:36:58 UTC完成；复用现有 Codex CLI 的 GPT-6 Astra / xhigh。输入是公开 system1-agents README 固定提交 `0f0b4c9eca03127875cfdf0d941fbd22c16478a4` 的节选，第三方视角、简短、普通帖＋串帖、中英文。材料不提供性能保证或生产验证。

真实调用完成共同母稿、X改写、中文本地化、两语各一次审核和修订，共7次成功CLI调用；英文与来源同语，`skipped_same_language`透传并冻结，没有虚计一次本地化调用。两语 `auditCount=1`，所有逐帖ID与实际计数已核对。稿件忠实保留决策选项、聊天模型职责与资料边界，没有性能提升或客户采用的虚构主张。未知的页面任务定义和完整仓库实测仍列为未解决资料问题；中文列表保留部分英文原始事实边界，有重复表述，不能宣称内容质量已经无需人工编辑。

| 语言 | 普通帖加权字数 | 串帖逐条加权字数 |
|---|---:|---|
| 中文 | 179 | 183、147、155 |
| 英文 | 254 | 261、210、233 |

真实局改任务 `7666c212-d5fb-4cba-b3e8-d86e824d5220` 完成；接受后的新tmp为 `e449f5c0-b1d7-4128-8477-deb52cef6661`，普通帖计数181，保留两份原始终稿。局改不再自动审核或生成图片，图片重新核对由操作者显式发起。

所有真实来源、正文、提示、审核、图片及模型输出均位于 `tmp/x-completion-real/data/tmp/`，不进入 Git。元数据断言记录在 `tmp/x-completion-evidence.json`：原中英文正文保持不变、仅目标句改变、另一对象不变、原图字节hash与绑定一致、正式内容为0。X发布记录为0。

## 真实后台图片

共享runner／worker来自已提交MVP `1e7cc37`。在用户已批准的正式Codex官方缓存边界内，经工作台后台生成，不使用会话出图工具或手工复制图片替代集成。

| 对象 | 生成任务 | 实际结果 |
|---|---|---|
| 中文普通帖 | `921b19fe-a5e8-4983-a9f4-bcfcb006fda1` | PNG 1536×1024，1,409,215字节，生成和实际看图通过 |
| 英文普通帖 | `89e45ef5-baa6-452f-b0ee-a626d66e5694` | PNG 1536×1024，1,144,645字节，生成和实际看图通过 |
| 中文串帖首图 | `c4d54ba7-8725-4929-8d67-56f555205d9f` | PNG 1536×1024，1,196,860字节，生成和实际看图通过 |
| 英文串帖首图 | `7a796717-2477-4de9-a09c-e3b379be56c1` | PNG 1536×1024，1,169,452字节，生成和实际看图通过 |

四张均为无文字机制概念图，每个对象单独生成并以其语言和实际正文检查，未把中文图直接当英文图通过。完成状态均ready；`source.kind=generated`、`madeWithAi=true`、`visualMethod=codex-image-input`，实际图片字节hash与对象绑定匹配。看图检查采用真实图片输入，findings为空；主Agent也逐张查看原图，形状／箭头可辨，无文字乱码、虚构数据或产品截图。图片是配合正文阅读的概念示意，不能当作真实系统结构截图或广泛质量基准。

改稿后的同图复核任务 `e53f49b2-42ee-44b1-a624-627c0f8107b0` 真实通过，新的正文hash重新绑定。没有自动无限重试，四张原图均首轮成功；失败与取消的保护由隔离替身与浏览器合成案例另行覆盖。

## 外部能力

| 项目 | 状态及证据边界 |
|---|---|
| 写作／计数／编辑／保存／发布适配代码 | 已实现，本轮自动及浏览器验证分别记录 |
| 后台自动配图 | 已接入，四对象真实生成＋看图检查通过，改稿同图复核通过 |
| 实际图片质量 | 本轮四张真实原图已由视觉模型检查和主Agent逐张查看；不外推为任意产品／题材都通过 |
| X OAuth／账号和权限 | 官方文档核查与替身协议通过，真实应用资源未准备，未真实验收 |
| X 真实媒体上传 | 未进行，仍需具体账号、版本、素材及发布授权 |
| X 真实帖子与线上显示 | 未进行，不能以合成 IDs 或接口文档宣称通过 |

普通操作者可检查暂存正文与实际图片。X连接仍需要负责人准备开发者应用、权限、额度和回调，然后由操作者在X页面授权。本机只读连接状态为unconfigured，缺Client ID和HTTPS回调；没有读取密钥或尝试真实发帖。配置与后续逐项验收见 [发布文档](publishing.md)、[操作者指南](../../public/docs/x-setup.md)。

README按本地应用的能力同步模式更新，保留原启动、备份和LinkedIn历史入口；修正X未支持／所有图片后台未接通的过时描述，并明确本分支未整体合并MVP。没有新建语言版本、徽章或外部素材，未声称代码已推送到公开仓库。
