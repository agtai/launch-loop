# LinkedIn MVP 本轮验收报告

验收起始日期：2026-09-24。开发基线`5942773`，分支`codex/mvp-integration`。2026-09-25 用户授权将本轮开发成果及待办记录纳入本地提交；没有push、合并、公开部署、代用户确认真实稿或真实LinkedIn上传／发帖。原工作区数据与其他任务服务未使用。下方历史“未commit”表述为当时状态，最新提交与验证状态见[会话交接](SESSION_HANDOFF.md)及[验证记录](验证记录.md)。

前次收尾决定：用户要求“最快闭环，剩余问题下次处理，现在验收MVP”。当时停止新增真实调用，交付本地工作台供人工验收。用户现已明确继续原延期项，最新状态见下一节；下方原始证据保留。

## 最新决定：连接按网络依赖搁置

2026-09-25 用户要求将本节状态明确记为待处理：后续恢复工作时载入最终服务改动，网络恢复后续验真实图片质量及双语图文绑定。当前只记录，不恢复探测、重试或服务重启。失败分类、审图意见保留、箭头与背景要求等本地修复已完成；已有双语正文仍保留在 tmp，未代用户确认保存或发布。具体待办清单见 [会话交接](SESSION_HANDOFF.md)。

用户明确要求先不纠结与Codex的连接。已停止相关诊断、探测和真实重试，不再重启服务触发探测。此决定不代表已经证明网络内部根因，也不将未通过的配图质量和双语绑定计为完成。

本地最后setup失败分类、受控错误映射及英译已收尾，40项定向通过；之前质量brief／脱敏修改后的216项全量通过。构建日志`tmp/mvp-image-local-final-build.log`，测试日志`tmp/mvp-image-local-final-tests.log`、`tmp/mvp-image-quality-all-tests.log`。最终源码及dist就绪，服务最后一批分类修改待后续重启载入，未commit／push。

15:08:42.386的修正配图任务`1a6747b9-4a48-4a8f-8059-fe51bad86679`在15:09:02.983以code1结束；account/read返回RPC -32603，无thread／turn／图片调用。故不计作新图片质量验证，也未进行双语图文绑定。此前71d3成功出图但未通过审图；两语文本仍在tmp，未正式保存或发布。

## 终止通知修复与图片质量复测（14:38 UTC起）

用户继续授权修复。公开app-server协议存在的终止`error`、`thread/closed`此前未被完整处理；新增精确thread／turn匹配、终止通知立即失败、可重试通知仅记录、保留首次错误。runner按白名单返回额度、授权、连接、提前关闭等受控提示，并用ContentError贯穿任务／语言／图片错误，中英界面一致。原始provider消息及敏感字段不进页面。216项自动测试、35项定向和构建通过，日志`tmp/mvp-image-terminal-*.log`。

进一步只读核对e529本任务SQLite日志，图片请求13:44:12 UTC返回HTTP200，但没有完整图片或完成事件；outgoing通知汇总无error，故不能认定上述终止通知缺口就是e529超时根因。当前本机代理TCP可达，显式代理公开站点可完成TLS；不改全局代理，不直接读取凭据或自行调用内部图片HTTP接口。连接可达也不代表出图完成。

14:38:32.879工作台发起真实任务`71d3b1cd-5798-4341-8a17-b6f219dec80d`。14:42:27.975生成worker以code0完成并收取新图；PNG 1,304,440字节，SHA256 `1fd87278b3ff818f663a9a1dd33339d700aca29bc89d76f424c0674d30016129`。主Agent实际看图。14:44:38.677视觉检查拦截了入站箭头误指未选中的物流选项、画面透明边缘彩色杂点，以及职责界定仍隐含等问题；未附图、未保存正文。

针对实际画面修正通用brief：输入箭头只到选择器边界，不指向未选项；唯一选中项到对应输出；不透明浅色底、干净边缘、留白和简化布局。没有写入产品专用标准图。审核提示明确把中性描述和已通过项放observed，只把需修正的具体缺陷放findings，仍须passed=true且findings空才可附图。修复错误路径脱敏误吞`reasoning/chat`、`logistics/package`自然语言；真实路径继续隐藏，原始诊断保留。28项图像与诊断回归通过，日志`tmp/mvp-image-quality-tests.log`。

14:58启动探测在account/read收到内部RPC错误-32603，未有图像调用；通过页面手动重查恢复available，未修改账号或全局网络配置。随后15:08再次按新要求执行一次真实配图，结果以本节后续完成证据为准。

## 续接延期项：入口恢复（13:42 UTC起）

配图入口原失败确认为30秒启动探测截止，随后被服务一直缓存为不可用。旧日志无法区分更深内部原因。新增安全阶段耗时、`probe_timeout`分类，以及显式“重新检查配图入口”按钮和`POST /api/generation/capabilities/image/refresh`；并发请求合并为一次检查，关闭时取消并丢弃迟到结果。保留30秒、一次尝试、工具隔离；没有自动生成或自动重试。`GET`仍只读，未启用provider时不能借此启用。

最终代码无模型实测`tmp/image-probe-restart-fixed-1`耗时11.915秒，登录和imageGeneration能力通过，0 thread/turn；另一次诊断探测21.317秒，首次initialize耗时16.349秒，说明冷初始化可能占较多预算，不是旧超时更深根因的证明。4348重新加载后启动探测及浏览器中英手动重查成功，任务数5、活跃0，无额外模型调用。

构建及210项自动回归通过，0失败／取消／跳过；33项图片定向测试通过。日志`tmp/mvp-resumed-all-tests.log`、`tmp/mvp-image-resume-tests.log`。13:42:25.743从原英文终稿发起真实图片任务`e529f113-36f5-477f-9135-71f3dbaa4738`，原正文未改，继承同稿同revision的`42a35207`两条视觉发现；质量和绑定验收需以下实际完成证据。

本次复测于13:49:26.359 UTC在420秒生成子阶段上限处终止（code124、timedOut=true、cancelled=false）；任务为failed。出现imageGeneration started，但没有图片完成／turn完成事件、result.json、generated.png或该thread的官方缓存目录。agentMessage完成不是图片完成，未发现漏收已完成图片的证据。stderr有5次模型列表刷新请求超时，不能据此断言图片生成停滞的内部原因。未自动重试、未放宽视觉检查、未上传旧失败图绕过检查。

图像质量和双语图文绑定仍待真实成功图片。原中英文tmp均revision3、confirmed=null、图片0，正文保留；未新增正式版本或发布。修复了本次暴露的诊断继承边界：中间一次无图的传输超时不能抹掉同稿同revision最近的真实视觉发现，重启后仍继承，检查模式和其他稿件不混用。最后8项反馈／能力定向回归通过，日志`tmp/mvp-image-final-regression.log`；全量最后结果见验证记录。

目前已处理原延期项中的入口状态恢复与诊断；剩余为出图完成及质量复测、双语图文绑定（正式保存由用户自行确认），以及具备应用／HTTPS回调后的真实OAuth和具体发布确认。用户随后新建英文任务`37ed7c76`的240秒平台改写超时属于另一次文本执行问题，本次未重跑或修改其预算。

最终211项全量重跑通过，详见[验证记录](验证记录.md)。机器核验`tmp/mvp-resumed-evidence.json`确认原双语正文与修订工件逐字一致、正式作品和发布记录0、活跃任务0，配图能力available。英文工作台显示实际图片超时原因与可读正文；控制台无warn/error。原main干净且仍5942773，无commit／push。

## 能力和证据边界

| 层级 | 当前结果 |
|---|---|
| 本地实现 | text-v2共同母稿与双语流程、保守改稿、独立后台图片、LinkedIn OAuth／预览／防重已整合 |
| 自动回归 | 构建与最终211项全量重跑通过，候选10项为前次证据；先前一次临时node.exe清理EBUSY失败及重跑边界见验证记录 |
| 真实文本 | 通过：一组中英文终稿、每稿一次有效审核、统一修订、一次真实局部改稿；此前失败及恢复全部保留 |
| 真实图片 | 旧PNG视觉不通过，未附图；入口重查恢复已验证。本次带诊断复测在420秒生成上限超时，无新图，质量及双语绑定仍未通过 |
| 真实OAuth | 未执行：操作者尚无应用及HTTPS回调 |
| 真实发布 | 未执行：还需操作者OAuth、选定正式版本及具体账号／正文／图片确认 |

接口、预算、tmp与备份兼容详见[MVP执行与素材接口](MVP执行与素材接口.md)。外部配置只缺一次性资源与操作者动作，见[LinkedIn最小配置](LinkedIn最小配置.md)。本地默认API版本和scopes不是已获平台权限的证明。

## 可重复执行

均在本分支项目根目录执行；路径相对项目解析：

```powershell
npm run verify:mvp
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\server\Start-MvpAcceptance.ps1 -Mode Real
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\server\Start-MvpAcceptance.ps1 -Mode Synthetic
```

`verify:mvp`依次执行npm ci、构建、全部测试、候选contracts和只读LinkedIn预检。缺应用配置明确标`DEPENDENCY MISSING`并退出1，不把整体外部验收包装成通过；各本地检查独立记录。模型和LinkedIn自动回归全部使用明确替身，不发真实帖子。

- 真实待审库：[http://127.0.0.1:4348/](http://127.0.0.1:4348/)，运行根`tmp/mvp-real/`。
- 合成验收库：[http://127.0.0.1:4349/](http://127.0.0.1:4349/)，运行根`tmp/mvp-synthetic/`。页面横幅明确标替身；1×1图片和合成平台ID只用于状态回归，不代表图像质量或线上成功。
- 启动脚本复制本分支server/rules/dist进对应隔离目录，更新时只停该目录登记的服务，保留其data；不会操作原项目运行实例。
- 停止真实库：`powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\tmp\mvp-real\Stop-Workbench.ps1`；合成库替换为`mvp-synthetic`。
- 本轮没有清理任何已有tmp。正式JSON备份仍不包含临时稿；保留本机待审内容需先停服务再完整保存对应data目录。

## 真实执行

材料为system1-agents固定commit`0f0b4c9eca03127875cfdf0d941fbd22c16478a4`的README节选，已实际下载、阅读并从创作页上传。原文和节选在`tmp/mvp-inputs/`，不进入Git。本次未读取节选中的所有外链、未运行产品代码或复测性能数字。目的为解释明确选项决策与聊天模型职责边界，目标为Agent开发者。

初次任务`adbb202f-fcb8-47ed-96d6-05fd9bc95118`于05:39:28 UTC开始。完成母稿、平台稿、本地化及两次审核后，独立复核发现：英文材料被模型以“明确中文语言选项”为由选择中文母稿。共享阶段混入首份目标语言的规则metadata，已去除，并增加反转目标顺序的payload一致性回归。于05:52:21 UTC停止修订调用，退出125/cancelled，不计作最终双语通过；没有进入图片生成。

修复后任务`a42c1120-84c5-4f43-89c9-d8096242eae6`于05:56:30 UTC从实际工作台发起。母稿于06:00:02完成，正确选择英文；平台稿随后在180秒预算耗尽，保留原失败。针对实际耗时，将平台预算有限调整至240秒并精简提示中的重复来源元信息；增加严格核对输入、来源、规则和工件的一次性母稿恢复。06:10:39从实际工作台点击继续，原母稿及intake逐字保留，平台稿于06:14:03完成（204.761秒），中文本地化于06:16:08完成。下游未复用第一份错误母稿。工件、来源、规则、完成事件、取消证据均在`tmp/mvp-real/data/tmp/generation/<jobId>/`，可读稿位于同库tmp/content。

安装／构建／171项当时全量回归及10项候选检查日志在`tmp/mvp-validation/2026-09-24T05-58-50-248Z/`。最终全量203项日志为`tmp/mvp-final-tests.log`；构建为`tmp/mvp-final-build.log`，候选为`tmp/mvp-candidate-latest.log`。独立母稿恢复7项与审核恢复8项日志分别为`tmp/mvp-resume-validation/test-output.txt`、`tmp/mvp-review-resume-validation/test-output.txt`。配置预检因缺LinkedIn应用退出1，未标成整体外部通过。浏览器看到恢复阶段真实开始时间、预算与复用说明，未重新执行mother。

同一真实任务的中文审核于06:20:08 UTC耗尽240秒（code124，无返回稿，stderr空）。配置警告同样出现在成功调用，不能据此判定超时根因。审核提示中有当前稿副本和较繁重元信息；保留全部规则、来源与账本，仅删除重复正文、精简manifest并要求短评短引文，实际输入缩减约3.2%，不能宣称已证实解决时延。审核预算有限调至300秒。增加严格冻结工件复用，在06:31:29 UTC由工作台显式继续未完成审核一次；已成功阶段、原工件及冻结稿revision不重写。后续结果以本报告最终状态为准。

续审结果：中文06:34:20、英文06:36:48完成；两份审核四项编辑判断均附真实引文，各7处位置核验通过，findings均为空。中文修订工件06:38:10.585、英文06:39:17.320完成。两语title/blocks、postingNote和claimBindings均逐字不变，changeStatus=unchanged，resolvedFindingIds/findingResolutions为空；没有把无改动说成提升。中文418、英文1017字符。独立质量复核在`tmp/mvp-quality-review.md`；typed values未定义、详细probe标准和独立性能证据缺失仍属内容限制，正文没有虚构这些证据。

真实局部改稿`0fbbfbd0-7094-4282-a7cc-583ddd78980f`从工作台选择英文第一段126字符并提交，得到更直接的同义表达。机器逐字比对确认选区外四段、原稿及事实范围不变，changesSharedFacts=false。界面原选区／候选并排验证后拒绝提案，保留原文，未代用户采纳或正式保存。证据为`tmp/mvp-real-modification-evidence.json`，候选tmp仍保留。

首次自动图片于06:39:52明确失败，两份终稿保留。真实stderr指出图片工具依赖的code_mode_host被适配器隔离开关关闭。仅对独立图片worker启用所需官方宿主，code_mode、shell、其他apps/plugins/MCP仍禁用并检查effective配置，文本执行器不变。官方宿主stdio启动/退出及新只读probe通过；定向34项（图片、边界、审核恢复、i18n）通过，日志`tmp/mvp-final-targeted.log`。06:43:47从英文工作台发起独立图片任务`42a35207-f867-472f-a460-03da51fdfbb0`，不重跑文本；此次已观察到正式imageGeneration开始事件。

该任务于06:45:02真实生成1254×1254 PNG，1,798,576字节，SHA256为`ad3a79ddec61caa0df4bb65577f4b3792273284b42e5796c6c417178e4b265eb`。主Agent实际看图；自动视觉检查于06:45:56发现System 1与chat model职责边界不明确，以及六个装饰点与五个队列的数量歧义。即使模型输出passed=true，只要findings非空，仍按失败拦截；图片仅留在`tmp/mvp-real/data/tmp/generation/42a35207-f867-472f-a460-03da51fdfbb0/image/generated.png`，未附入两语稿件。

已实现显式配图重试继承同一tmp、同一revision的最近失败诊断，并在新构图中修正；跨稿／改稿／仅检查模式不继承。错误提示显示有长度限制并隐藏路径的具体发现，原诊断完整保留。图片18项及诊断继承3项定向测试通过，随后全量203项通过；最新真实服务已加载修复。遵照用户即时验收要求，未再点击生成，未做修复后真实出图或中文复用图片检查；这是下次的首要事项。

收尾刷新后的能力提示为“后台 Codex 配图入口暂不可用”，生成图片按钮禁用。此前真实出图证据仍有效，但重启后的能力探测稳定性同样留到下次排查，不宣称当前图片入口已稳定就绪。交付时活跃任务0、真实正式作品0、真实发布记录0。

## 浏览器验收

实际使用Codex浏览器操作两个本机地址。以下“替身”不等于真实外部服务：

| 检查 | 执行证据与结果 |
|---|---|
| 必填、资料、配置、双语UI | 空表单显示三项必填；真实文件从文件选择器上传；未适配平台禁用生成；中英文切换不改稿件正文 |
| 生成防重复 | 合成表单双击只产生一组任务；进度来自服务阶段，不先显示最终完成 |
| 修改／拒绝／接受／撤回 | 原选区和候选并排；拒绝原文不变；接受新tmp；过期图阻止保存；撤回生成反向候选并明确接受 |
| 正式版本 | 仅合成稿确认；双击保存仅版本1；修订配图后保存版本2，再读取版本1得到原正文与原素材URL |
| 发布预览 | 合成授权账号ID、版本号、完整正文及对应图片一致；纯文字版本明确无图；未确认tmp不在作品选项 |
| 重启／重开 | 自己的合成服务重启后授权断开，已确认版本与发布防重记录保留，不自动发布 |
| 取消／超时／图片失败 | 浏览器取消慢替身；有界超时替身终止且无图；图片失败双语文本仍可读，显示失败原因 |
| 授权失效 | 一秒有效期替身明确过期，预览按钮禁用；中英文提示均验证 |
| 未知发布结果与防重 | 合成版本4返回HTTP503且无平台ID，记录为unknown；再次明确点击提交被拒绝，未增加提交；重启／恢复后unknown记录仍在 |
| 并发改稿与语言同步 | 慢提案期间手动改正文，迟到建议被拒绝且手改保留；接受共同事实提案后另一语言不变，显式同步生成独立候选，拒绝仍不覆盖 |
| 恢复前编辑竞态 | 合成稿修改后立即点击续审，界面先persist，服务以源稿revision变化拒绝；原手改保留，reviewResumeCount仍未消耗，未产生额外review调用目录 |
| 审核等待状态 | 已成功审核的语言稿使用reviewed状态，等待其他稿审核及统一修订；状态修复后独立恢复测试8/8定向复测通过 |
| 刷新、恢复及计数 | 清空测试表单后实际刷新，16份tmp和1份合成正式作品保留；新生成后计数自动14→16，无需手动刷新；恢复合成备份成功，旧tmp只读并提供显式派生入口 |
| 页面尺寸、控制台与英译 | 默认1280和390×844实际看图，正文无横向溢出；窄屏历史导航可横向滚动；恢复、未知结果等英文提示无已知漏翻译；最终测试标签无warn/error，临时视窗覆盖已恢复 |

浏览器首次未知结果替身未触发，原因是测试sentinel没有考虑LinkedIn little-text对下划线的转义；已修复替身判断，保留此前合成记录，不能把该次“Published”算成未知结果验收。无真实网络发布。

## 已发现并修复的问题

- 共享母稿误用首份目标语言：共享阶段移除language选项与language fragment，单独记录共享规则hash；完整每稿规则快照仍保留。
- 相同规则不同批次可能同步：验证原生成血缘，禁止跨批读取账本或同步。
- 撤回可能命中相同文字的错误位置：要求原偏移及邻接锚点匹配，只派生反向候选。
- 图片检查无选图误落生成、独立图片状态及迟到结果、恢复后图文绑定：分别修复，增加独立边界回归。
- 后台能力探测偶发SQLite初始化失败：发现MCP隔离重启未等待旧app-server完全关闭，改成总预算内最多3秒等待准确child close后才启动；修复后真实只读probe成功。原stderr未给底层SQL原因，不据此断言所有SQLite故障根因都已证明。
- 稿件库计数、能力探测文案轮询、修改后图片状态与错误英译补齐。合成服务额外显示永久测试横幅。
- 有界母稿／冻结稿恢复保留成功工件；恢复前等待当前编辑暂存，避免误耗唯一继续机会。已成功审核不重跑，恢复不增加重复执行记录。
- 图片生成与自动看图统一筛选平台建议，忽略旧文字建议和内部hash；实际双语正文逐稿核对，不能因仅匹配一语就通过。未用提示窄修代替真实看图证据。

文本、图片和LinkedIn由不同Agent按文件边界实现，关键整合由未编写对应模块的Agent复核；主Agent处理有效发现并复测。没有降低断言、删除失败用例或把测试替身标成真实外部结果。

## 人工验收范围

现在可直接验收：

1. 打开真实待审库4348，在“生成与审核”任务中切换中文／英文，阅读“源语言修复复测”两份终稿。保留的失败任务是诊断历史，不代表终稿丢失。
2. 选中一段文字体验修改，检查原文／候选对照，再自行选择接受或拒绝。新模型调用需要等待；本轮真实改稿证据已经保留，不必为验收重新生成整篇。
3. 检查配图失败时正文仍完整，以及“确认保存”与发布分开。是否正式保存由操作者决定；当前真实库正式作品和发布记录均为0。

后续继续：排查真实出图未返回结果的原因，完成合格图片与双语绑定；随后按[LinkedIn最小配置](LinkedIn最小配置.md)准备应用及仅回调HTTPS资源，完成正式OAuth。真实上传／发帖仍需另行确认具体账号、正式版本、完整正文和图片。
