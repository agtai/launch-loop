# Launch Loop：会话交接

## 当前 main：X 与 LinkedIn MVP 整合（2026-09-26）

用户已授权把 X 独立分支的唯一提交 `083bd70` cherry-pick 到 `main`，基线为已包含 LinkedIn MVP 的 `8bc5bb7`。本次只新增一个整合后的提交，来源提交与新提交 hash 不同；通过 Git 提交说明中的 cherry-pick 来源追溯。没有 push、复制运行数据、真实模型／图片调用或重启已有服务。

共享冲突同时保留 LinkedIn `text-v2.0.0` 生产候选规则、母稿恢复、改稿接受／拒绝／撤回／同步、图片正文绑定和发布核对，以及 X 双格式、逐帖编辑、独立图片任务与正式授权发布适配。混合任务按平台组合规则并分别运行，不误把 X 套入 LinkedIn 候选流程，也不宣称跨平台共用母稿。新构建随源码提交；本次隔离自动测试与浏览器结果见 [验证记录](验证记录.md)。

下节 X 真实验收证据来自来源工作区；其 `tmp/x-completion-real/` 不在 main 中，须用 `git worktree list` 找到 `codex/x-content-publishing` 工作区查看。LinkedIn 图片质量／双语绑定与两平台真实 OAuth／上传／发帖的既有待办仍分别保留，不因代码合入而记为通过。

## X 来源分支记录（2026-09-26）

本分支 `codex/x-content-publishing` 从已提交 `5942773a2e7372af461497185717a06553fe2444` 隔离开发。用户于2026-09-26要求完成 X 并形成一个本地 commit；没有 push、合并或真实上传／发帖。按已明确的 Codex 官方缓存决定，从已提交 MVP `1e7cc37` 复用图片 runner／worker 和启动开关，没有复制另一工作区的未提交修改。最新入口为 [X 交接](x/handoff.md)、[验收记录](x/validation.md)、[接口与合并边界](x/implementation.md)。下方 LinkedIn 历史证据保留，最新授权与图片状态以上述本轮记录为准。

已实现 X 普通帖与稳定逐帖串帖、中英文同包双格式、官方加权计数、共同母稿→平台改写→本地化→每包一次审核→修订、tmp编辑与局部改稿、图片关联及目检确认、正式版本选择、OAuth／媒体适配和多步发布防重。LinkedIn 保留原生产规则；X 与 LinkedIn 混合请求尚未统一为跨平台共同母稿。

本轮真实看稿入口是本分支 `tmp/x-completion-real/` 的独立端口 4356，所有真实正文、图片和审核只留其 `data/tmp/`。X 图片后台已接入，启动参数为 `-EnableCodexImages`；正文和图片任务分别显示结果，取消／失败不覆盖正文。自动／浏览器／真实模型的准确结果以链接报告为准。真实 X 应用及授权资源未准备，未做真实媒体上传、发帖或线上显示验收。合成浏览器账号及合成平台 IDs 仅用于内部测试。下一位操作者先检查暂存稿，再由负责人按 [X 指南](../public/docs/x-setup.md) 配置账号连接。旧 `tmp/x-browser/` 的9月24日任务已按用户“停止”要求取消，未自动续跑。

## 本轮本地提交（2026-09-25）

用户授权 commit，本提交收录 `5942773` 之后的 MVP 开发成果、配套构建、验证与操作文档，以及体验改造／配图真实验证待办，分支为 `codex/mvp-integration`。未 push、合并 main 或对外发布；真实稿件、图片、运行数据、凭据和临时日志不进入 Git。提交标识以该分支 Git 日志为准，下文“未commit”等表述保留为当时状态。

提交前 `npm ci`、`npm run build` 通过；完整测试首次 216 项通过、1 项在 Windows 临时 node.exe 清理时 EBUSY，未改代码完整重跑后 217 项全部通过。细节见 [验证记录](验证记录.md)。没有重启真实工作台、探测账号或发起真实调用；最后一批服务改动仍需后续载入。已记录的体验改造尚未实施，真实图片质量／双语图文绑定与 LinkedIn 外部验收仍未完成。

## 补充待办：配图真实验证（2026-09-25）

用户指定将此前“网络搁置、本地修复已收尾、图片质量和双语图文绑定待真实验证”的结论记录为待处理。本次仅记录，不恢复探测或真实重试。

- 已完成：失败原因分类、审图意见保留、通用箭头与背景要求修正；上次构建及最后 40 项相关测试通过。本次未重跑验证。
- [ ] 后续恢复工作时载入最终服务改动。上次收尾时 4348 尚未加载最后一批分类修改；重启会触发能力探测，目前仍不执行。
- [ ] 网络恢复并续接任务后，按修正后的要求真实生成、实际检查图片质量，保留通过或不通过的证据。
- [ ] 使用合格真实图片验证中英文正文分别绑定、预览及正文变更后的绑定失效保护；完成前不标记通过。

已有真实双语正文完整保留在 tmp，不从头重跑，不代用户确认保存或发布。网络连接继续搁置；本地修复与测试通过不等于真实图片质量通过。历史证据及该待办同步见 [MVP验收报告](MVP验收报告.md)。

## 最新记录：体验改造待办（2026-09-25）

用户要求“记录”上一轮评估。已将结论与建议顺序保留为后续待办；本次仅记录，不启动实施。目标为 Easy、Hands-off、Transparent，并支持用户随时介入。当前结论为部分满足，不能将文本阶段可验收等同于完整 MVP 达标。

建议待办顺序：

1. 当前成果与下一步优先展示，同一内容任务的历史尝试折叠保留。
2. 将规则、来源、审核及修订关系整理为可读的过程与依据。
3. 补齐暂停、调整、继续；保留有效成果，仅重做受影响阶段。
4. 配图独立处理，明确自动配图／上传／纯文本选择；配图期间允许编辑正文并保护版本绑定。
5. 统一异常恢复与原输入复用，精简日常界面和首次配置指引。

完整证据、优先级分组及验收标准见 [MVP体验审计](MVP体验审计.md)。确认保存与确认发布继续保留；网络排查仍搁置，真实图片质量／双语绑定及 LinkedIn 外部验收仍单列未完成。记录不等于功能已实现，也不包含 commit、push 或对外发布。

## 当前评估：Easy / Hands-off / Transparent（2026-09-24）

用户要求检查当前 MVP 是否容易使用、能够放手执行、过程透明且随时可介入。已只读核对 4348 创作／发布界面、实际审核展示、源码与既有验收证据，结论为部分满足；运行中只有取消及有限失败续接，尚无通用暂停／调整／继续，历史执行与 JSON 审核展示也不等于面向操作者的透明。具体证据、阶段控制矩阵、改造顺序和验收标准见 [MVP体验审计](MVP体验审计.md)。其中改造为建议，尚未实现，不增加开发授权。未进行新模型／图片调用、保存、上传、发帖或服务重启；继续遵守下节搁置网络排查的决定。

## 当前决定：网络连接暂不排查（2026-09-24）

用户明确“先不纠结和codex的连接了，是网络问题”。据此停止连接诊断、能力探测和真实出图重试；不改代理、登录或全局配置，不重启服务触发新的启动探测。以下在途调试已收尾，今后以本节为准。

本地修复完成：终止error／thread关闭立即结束并保留安全分类；连接准备失败与实际图片失败分开；受控错误贯穿任务／语言卡片／图片状态；修正通用图片brief的箭头走向、不透明背景和干净边缘；视觉findings仅容纳需修正的问题，仍需passed=true且findings为空；脱敏不再吞掉reasoning/chat等自然语言，真实路径继续隐藏。前次216项全量通过；最后setup分类及错误映射后的40项定向通过，日志`tmp/mvp-image-local-final-tests.log`。最终构建日志`tmp/mvp-image-local-final-build.log`。

真实证据：71d3于14:42成功生成新PNG，但14:44审图确认箭头误指与边缘杂点等问题，未附入稿件；通用brief修正后的1a6747b9于15:09在account/read的RPC -32603失败，未开始出图。不能把提示修复当作质量复测通过。原两语终稿保留，质量及双语图文绑定待网络恢复后再验；正式保存由用户自行确认。LinkedIn仍等应用／HTTPS回调和操作者授权，实际上传／发布仍另行确认。

最新源码及dist已在独立worktree完成，未commit／push；为遵守停止连接探测的要求，本次最后setup分类收尾后没有重启4348。后续要载入全部最终服务改动，按README的隔离Real启动脚本重启（它会启动能力探测）；不自动恢复任何失败任务。

## 再续配图超时修复（2026-09-24，14:38 UTC起）

用户再次要求“继续修复”。已按本机公开app-server schema修复worker遗漏的终止`error`及`thread/closed`通知：只响应当前thread／turn，服务端明确不重试时立即失败，不再空等总预算；可重试通知仅记录、不发新调用。图片结构化失败保留安全分类，首个错误不被进程退出覆盖。图片runner把受控错误作为ContentError返回，任务、语言卡片、图片状态和英文界面显示同一具体原因；不暴露服务端原始message、账号、路径或headers。

构建、35项定向与216项全量通过，日志`tmp/mvp-image-terminal-build.log`、`tmp/mvp-image-terminal-tests.log`、`tmp/mvp-image-terminal-all-tests.log`。新代码已载入4348，14:38:32 UTC从原英文终稿发起`71d3b1cd-5798-4341-8a17-b6f219dec80d`，继承`42a35207`的两条视觉意见；最终结果看验收报告，不能从启动或HTTP200推断出图成功。

对上次e529自身SQLite日志的进一步核查：13:44:12图片请求返回HTTP200，但没有完整图／完成事件；outgoing日志类型中无error。故这次终止通知修复是确认存在的代码缺口，不能认定为e529的确切根因。初次成功图和失败图的安全摘要`tmp/image-timeout-comparison.json`；不导出原始headers或凭据。当前系统代理TCP可达，显式代理公开站点TLS连接可完成；这些不等于图片API完整成功。

## 继续处理原延期项（2026-09-24，13:42 UTC起）

用户已明确“现在继续处理之前的问题”，覆盖下方即时交付后暂停状态。本次只续接当时延期的配图入口稳定性、带诊断的真实质量复测、双语图文绑定，以及资源具备后的LinkedIn联调；不从头重跑已有成功文本。用户验收时新建英文任务的240秒平台阶段超时是随后发现的独立问题，不并入当时延期清单。

启动探测失败工件`image-capability/d5ded075-be1b-4848-9d17-452b81bb6f07`确认30秒总预算耗尽，旧日志无阶段信息，不能判断更深内部原因。图片worker现增加不含凭据的阶段耗时和`probe_timeout`分类，保留30秒及全部隔离。修复后两次无模型探测成功，最后一次11.915秒；未启动thread/turn或生成图。增加显式“重新检查配图入口”：并发合并、关闭取消和迟到结果保护，不自动重试、不发模型请求。4348已加载，浏览器启动检查及中英界面手动重查成功；任务仍5个、活跃0后才开始下一项。

构建及210项自动测试通过（0失败／取消／跳过），日志`tmp/mvp-resumed-all-tests.log`；图片相关33项通过，日志`tmp/mvp-image-resume-tests.log`。真实配图复测任务`e529f113-36f5-477f-9135-71f3dbaa4738`从原英文终稿发起，确认继承原`42a35207`的两条视觉诊断。最终结果见[MVP验收报告](MVP验收报告.md)最新节，不能将进行中状态算作质量通过。

该配图于13:49:26 UTC达到420秒生成上限，code124，无图或完成事件。入口重查已修复且实测恢复；出图完成、质量及双语绑定仍未验收。日志有模型列表刷新请求超时，不能证明图片停滞的内部根因。未追加生成或放宽检查；新增一次传输超时后仍继承最近真实视觉发现的边界修复，8项定向回归通过。中英文tmp均revision3、confirmed=null、图片0，正文未改；正式保存继续由用户操作。

最终全量211项重跑通过，日志`tmp/mvp-resumed-final-recheck.log`；前一次临时node.exe删除EBUSY失败保留于验证记录。最新代码已加载4348，能力available、活跃任务0、正式作品／发布记录0；`tmp/mvp-resumed-evidence.json`核验双语正文逐字等于原成功终稿。原main干净，未commit／push。此轮没有合格新图或LinkedIn资源，因此剩余质量／绑定／外部联调不能算完成。

## 当前轮：LinkedIn MVP 整合（2026-09-24）

当前基线`5942773`，独立分支`codex/mvp-integration`。用户要求A技能接入、B后台配图、C LinkedIn准备、D自动及浏览器／真实验收，覆盖下方历史限制。全部成果未提交；不commit、push、合并、部署、代用户确认真实稿或实际LinkedIn上传／发帖。原工作区和其他任务的数据／服务不操作。

用户已明确批准复用已登录Codex的图片能力及官方缓存例外，无需再问入口。用户尚无LinkedIn开发者应用和HTTPS回调，要求本地实现及[最小配置步骤](LinkedIn最小配置.md)。真实OAuth、上传及发布继续待操作者资源与具体最终确认。

新任务使用`text-v2.0.0`：共同母稿→源语言LinkedIn稿→同语透传／另一语本地化→冻结→每稿一次审核→收齐后修订。三项必填不增加，旧v1及候选原包保留。改稿使用原批次账本；接受／拒绝／撤回与显式同步保护原稿。后台图片独立适配，包含缓存检查、实际看图、正文hash绑定和恢复核对。契约见[MVP执行与素材接口](MVP执行与素材接口.md)。

最终证据、失败与修复后复测状态集中在[MVP验收报告](MVP验收报告.md)，不沿用下方99项或六阶段历史结论。本轮真实库`tmp/mvp-real`端口4348，合成库`tmp/mvp-synthetic`端口4349，均只监听127.0.0.1；启动／停止见报告。真实稿始终tmp，合成保存和替身发布不混入真实库。

已修复共享阶段目标语言污染母稿选择、跨批次同步、撤回错位、图片检查误生成、图文binding备份恢复、迟到图片及状态、app-server重启竞争。真实双语终稿及一次局部改稿通过；母稿／冻结审核恢复及原失败工件保留。最终203项自动测试和10项候选检查通过。

用户最新要求“最快闭环，剩余问题下次处理，现在验收MVP”。已停止新增真实调用，保留4348待审库供用户操作。官方图片后台已真实生成PNG，但视觉检查发现职责边界及数量歧义，正确拦截，图片未附入稿件。带具体诊断的重试修复已开发、回归并载入服务，尚未真实复测；下次先完成该项及双语图文绑定，再按资源准备情况进行OAuth。本轮没有完整图片质量闭环或外部发布验收。不要从头重跑已成功文本，不自动恢复或发帖。以下均为前轮历史。

交付刷新后配图能力提示暂不可用，按钮禁用；下次还需排查重启后能力探测稳定性。用户验收页已打开中文真实终稿，活跃任务0，正式作品／发布记录均0。原main仍干净，HEAD为5942773。

操作者逐步使用及填写验收结果见[MVP从零到一操作记录](MVP从零到一操作记录.md)，包含立即看现有稿和完整新建流程两条入口、按钮操作、预期结果、当前未完成项、备份与后续发布步骤。

用户随后要求准备英文版MVP：已将验收浏览器界面切换English并打开已有英文终稿，补充[英文操作与验收指南](MVP_USER_GUIDE.en.md)和[英文LinkedIn配置指南](LINKEDIN_SETUP.en.md)。按钮名称按实际英文词典核对；现有作品名、来源、正文与历史证据保持原文，未修改全局稿件语言默认值。此次仅准备英文验收入口和文档，未追加模型调用、正式保存、发布或既有待办开发。

## 技能候选拣选到当前分支（2026-09-24）

用户明确要求将`11526bc`放到当前`main`分支。本次基于`0dc93a2`拣选该提交，保留`skill-preparation/linkedin-text/`候选包与下方原技能分支交接记录，同时保留第三轮实际工作台验证记录。候选版本为`linkedin-text-candidate.0.2.1`，仍未接入生产；当前执行器继续使用`text-v1.0.0`。没有复制来源worktree的tmp正文、运行数据或凭据，没有push或实际发布。新的提交hash以Git为准，来源提交保留在原分支。

## 最新状态：第三轮自主工作收尾（2026-09-24）

以本节和[第三轮结果](第三轮接入与真实验收.md)、[最新验证](验证记录.md)为准；下方第二阶段／i18n记录为历史。本轮开发基线`53b1dc9`。用户在开发收尾后追加授权，将本轮源码、文档、public和dist整理为一个本地commit并展示全部提交记录；该授权不包含push或实际发帖。提交主题为`fix: stabilize generation and LinkedIn OAuth workflow`，具体hash以Git为准。后续先检查Git，不丢弃已有工作，不重复建设底层。

- 真实文本链路已通过：实际界面上传system1-agents固定版本节选，任务`7a48bd1f-bdb3-4c44-9181-c6afda33412b`完成中英文各初稿、一次审核、修订共6次调用。真实局部改稿、接受前不覆盖、接受后新tmp、手动编辑、语言切换、迟到结果拒绝和取消验证通过。规则仍text-v1.0.0；没有性能测试或平台审核完成的声明。
- 文本网络不再概括为“尚未恢复”。当前代理下最小调用、精简schema和完整六阶段已有成功证据；大输入仍出现一次300秒超时，stderr无根因证据。新增`Start-Workbench.ps1 -UseSystemProxy`只为本次进程复用当前手工系统代理，不改全局设置；保留进程隔离、超时和本次完成事件检查。
- 后台自动配图仍未接通。Codex内置入口会先写官方缓存，用户只澄清交接方式，没有批准缓存例外或选择API方案；不得据此自动运行。独立API缺账号配置且涉及单独费用。详见第三轮候选表。
- LinkedIn正式应用仍未配置，账号／scope／OAuth均未真实验证。已修复OAuth安全303回到发布页、实际scope解析、失效状态及双语错误页；配置步骤见[授权指南](LinkedIn授权配置.md)。只代理HTTPS回调，不公开工作台。未上传LinkedIn图片、未实际发帖。
- `npm ci`、构建与99项本地隔离测试通过；真实任务在`tmp/round3-browser`（端口4336），证据在`tmp/round3-validation`。正文、审核及诊断仅tmp；正式内容／备份内容／发布记录均0，没有替用户确认保存。确认保存、不可变版本、恢复与发布防重由本轮自动测试验证，替身不算外部验收。
- 用户澄清：开发和首轮使用后，第二位负责人在其电脑承担全部生成和发布，内容放其项目目录；按最新要求保留自动创建的项目相对`data/tmp/`。其他人只读查看是待实现需求，当前仍仅本机监听；不复制操作者凭据。Codex只是当前适配，不是永久唯一供应商决定。

后续依赖用户的事项是图片入口／缓存边界选择、LinkedIn实际应用与HTTPS回调资源、操作者正式OAuth；这些完成后再准备用户已确认版本的完整预览。实际发帖仍需用户另外确认具体账号、版本和配图，默认仅其选择的一份。当前不能宣布完整MVP打通，不自动扩展其他功能。

收尾保留隔离端口4336的服务和看稿页面，供用户检查真实tmp，未启动自动重试；其重启／停止方式见第三轮文档。授权失效的浏览器回归使用另一个明确合成账号服务，已结束；该测试不是实际LinkedIn授权或发帖。

## 前轮记录（保留证据，状态以最新节为准）

当前授权更新：2026-09-24，用户在已提交的中英文界面基线 `53b1dc9` 后启动真实文本／改稿、后台配图与 LinkedIn OAuth 联调。开始时工作区干净，i18n 工作已提交，不存在未提交并行界面改动。以[第三轮范围](第三轮接入与真实验收.md)为准推进，以下历史“完成后等待”不阻止本轮工作。本轮不 commit／push，不替用户确认保存或发布。
## 独立技能深化分支原交接（2026-09-24，来源分支历史）

原技能分支由已提交 `53b1dc97793371e5f24e84b772a10258695499e6` 建立，分支名 `codex/skill-deepening-linkedin`。这是与123并行的独立技能准备，不接续真实生成修复、后台配图或OAuth联调开发；以下保留原分支的授权及验证边界，当前工作台状态以上方第三轮记录为准。

候选入口：[LinkedIn文本技能](../skill-preparation/linkedin-text/SKILL.md)；[本任务交接](../skill-preparation/linkedin-text/HANDOFF.md)；[本次验证](../skill-preparation/linkedin-text/validation.md)。新增共同母稿→平台改写→本地化→每稿审核一次→统一修订的规则、选项映射、来源取舍和改稿协议，标记“候选、尚未接入生产”。用户追加授权将本批成果整理为一个本地commit，范围仅技能准备目录与本交接；生产src/server/rules和原始参考保持基线，不push、合并或发布。真实衍生工件仅在新worktree被忽略的隔离tmp，不进入提交；准确验证状态见链接记录。

独立任务已完成首轮真实旧/新规则双语CLI对照、局部改稿，以及追加的0.2.0/0.2.1写作质量校准。当前候选0.2.1，尚未接入执行器。新增37次成功CLI调用涵盖同资料两轮产品双语稿、三个不同题材/目的的双语留出和一份既有稿编辑挑战；仍为gpt-6-astra/xhigh。两次母稿300秒超时保留记录，相同提示在验证脚本延长上限后成功；未改生产预算或配置。npm ci/build、76项基线测试和最终10项候选检查通过。全部正文、审核与详细评阅只在新worktree的 `tmp/skill-deepening/`，未正式确认保存；不改变123的实际验收状态。

用户质量门槛为初稿至少达到launch-kit、审核修订后至少达到launch-copy对应内容水平，不要求相同，不得靠hardcode复刻。实际补强了读者判断、信息取舍、解释关系和单次编辑审核，没有加入固定段数、CTA或产品专用标准答案。最新真实英文稿达到同目的对应机制说明的可比编辑水准；两语终稿原样保留，不能声称审核令它提升。留出仍暴露一个英文语境遗漏；稳定性与自然生成后的审核召回尚未证明。详见[质量标杆](../skill-preparation/linkedin-text/quality-target.md)与验证记录。可读最新稿和全部对照位于 `tmp/skill-deepening/quality-round/LATEST.md`、`READING.md`、`assessment.md`，不进入Git。

更新：2026-09-24。基线第一阶段提交为 cd81286。用户已明确授权第二阶段开发，覆盖“第一轮结束后等待”；本阶段实现创作工作台、真实文本执行和LinkedIn普通feed适配。用户于2026-09-24追加授权将本阶段成果commit到本地Git，**未授权push或实际发帖。** 不自动扩展其他平台、视频、DOCX、kit/copy或反馈。

第二阶段已本地提交 `4b42ab9`。随后用户授权“工作台增加中英文切换，切换后变化所有 UI 内容”；本次仅实现界面语言切换，不重复第一／第二阶段开发和外部模型／发布验收。完成后用户于2026-09-24追加授权将中英文界面成果commit到本地Git，未授权push或对外发布。

## 中英文界面

顶部与详情弹窗内提供中文／English 开关；所有页面的导航、表单、帮助、状态／受控错误、日期、内置任务和资料描述随语言切换。`src/i18n.tsx` 提供 React 上下文，`src/i18n-core.mjs` 提供纯显示翻译及日期格式，`src/locales/*.en.json` 保存英文词典。浏览器只保存 `launch-loop.ui-language`，没有新增数据库字段或迁移。

切换语言不卸载创作界面、不重置未保存输入、不触发生成／修改／正式保存／发布。稿件语言选项独立保留，用户正文、标题、上传材料、真实审核发现、负责人和用户执行记录保持原文。历史模块只翻译与内置种子逐字段相同的默认描述；写回 API 使用原始编辑状态，不把显示译文覆盖数据库。任务与资料 JSON、原始参考和来源 URL 不改。系统内置警告单独翻译，模型审核正文保留原文。新增文案应接入词典，避免在渲染时保存已翻译状态。

本次 npm ci、build、76项自动测试通过；浏览器验证全页面英文、中文回切、未保存表单／弹窗输入、正文 tmp 自动暂存、刷新记忆、跨标签同步和390px窄屏布局。使用新隔离目录 `tmp/i18n-browser`、端口4334，没有使用真实data。验证必填提示时测试工具未按预期清空字段，误触发一个隔离生成任务，已取消且无正文产出，不算模型验收；详情与证据见[验证记录](验证记录.md)。本轮未实际发布，配图入口、LinkedIn授权和第二阶段网络阻塞的待验收状态不变。

## 先读与当前状态

先读 [AGENTS](../AGENTS.md)、[第二阶段范围与契约](第二阶段接口与分工.md)、[执行与发布接口](第二阶段执行与发布接口.md)、[内容存储接口](内容存储接口.md)、[固定规则](../rules/text/v1/README.md)、[验证记录](验证记录.md)和[README](../README.md)，再核对Git与实际源码。原[完整需求](内容生产与发布需求.md)和[创作讨论](创作模块设计讨论.md)中的历史暂停／第一轮大验收不覆盖当前授权边界。

采用当前任务内主Agent＋三个subagent，均GPT-6 Astra；主Agent Xhigh，界面High，执行与发布复杂一致性工作Xhigh。没有创建独立用户session或扩编。主Agent维护共享类型、路由、依赖、文档、public、dist与整合验收。

## 已实现与外部证据

| 范围 | 当前实现 | 验证与限制 |
|---|---|---|
| 界面 | 创作／发布／空反馈；三项必填带说明；实际上传；右侧中英文看稿、手动编辑、局部修改提案与确认保存 | 原任务、资料、研究／审查记录保留；浏览器验收详见验证记录 |
| 数据层 | 复用第一阶段tmp、revision、不可变版本、素材、恢复epoch和事务 | 未确认正文／素材／提示／审核只在tmp，不进入正式备份；不清理已有tmp |
| 文本执行 | 工作台HTTP到受控Codex CLI；平台×语言各初稿、一次审核、修订；来源实读、规则解析、取消、超时、幂等与重启恢复 | 普通测试不调用模型；真实CLI结果单独记录，不能用第一阶段预检替代本轮验收 |
| 配图 | PNG／JPEG真实上传与模型图像输入，版本内保存并预览 | 自动配图后端未连接；复用第一阶段会话图只算本轮上传，不算自动配图成功 |
| 发布 | 正式OAuth、个人普通feed单图、明确正式版本、独立确认、防重复和unknown恢复 | 未配置实际应用／账号；无真实OAuth、图片上传或发帖验收；没有发帖授权 |
| 其他 | 五个平台一致显示，text-v1.0.0沿用 | 其他四平台、原生Pulse、视频、DOCX、kit/copy待后续；反馈留空，完整MVP尚未打通 |

第二阶段可在本机完成的实现和验证已收尾：npm ci、build通过，最终72项自动测试通过；浏览器验证真实资料上传、失败状态、已有试稿的中英文看稿／真实图片／手动编辑、两次确认保存保留旧版、正式作品发布选择、恢复后tmp失效与显式派生。**真实模型验收未通过**：两次从工作台发起双语任务均在首个中文初稿调用网络超时，后续审核／修订和英文未执行；没有本轮真实正文产出。多站点、普通Node、受控宿主及Windows curl均在TCP连接阶段失败，支持当前网络限制判断，但未定位具体网络配置原因。停止继续模型重试；网络恢复后再验证真实双语三阶段及局部改稿。不能把本轮称为完整MVP通过。

## 必须保持的产品决定

- 直接进入创作，三项必填仅产品资料／已有内容、写作目的、目标读者。平台、格式、语言、作者身份、风格术语、篇幅深度、参考稿可选；试运行预设LinkedIn／中英文／普通动态可改，不是最终全局默认。
- 仅平台×语言增加版本数量，其余多选合并要求。五个平台为LinkedIn、X、小红书、知乎、Bilibili；pending配置明确拒绝，不套用LinkedIn规则。
- 固定生成初稿→自动审核一次→修订，用户不手选skill。未解决问题保留，审核完成不等于用户确认保存或平台审核通过；CTA和未定交互仍待定。
- 有选用图片则结合创作，没有则走默认生成素材意图；入口缺失显示真实限制，不能用占位图或提示词冒充图片。
- 自动暂存只到tmp；确认保存产生正式版本，已有内容的新修改先tmp，确认后增版且保留旧版。发布需要另行确认具体账号、版本和素材。
- 原始参考中的账号、日期、联系对象和历史命令不是执行授权；不读取历史密钥，不购买服务，不使用网页自动化绕过LinkedIn API权限。

## 可直接使用的接口与文件

- 创作页面：[ContentWorkspace](../src/ContentWorkspace.tsx)；发布页面：[PublishingWorkspace](../src/PublishingWorkspace.tsx)；共用类型：[content-types](../src/content-types.ts)。
- 生成：GET capabilities；POST／GET /api/generation/jobs；GET任务、POST cancel；POST /api/generation/modifications；POST任务accept。修改必须携带源revision和精确选区，接受建议生成可编辑tmp，不直接确认。
- 存储：沿用 /api/content/tmp 的读取、更新、素材、fork、confirm；正式作品读取、历史版本和revise。无需重建第一阶段存储或规则。
- 规则：[resolveTextRules](../server/rule-resolver.mjs)返回ready／pending／needs_configuration／invalid，只有ready执行。通用、中英文、LinkedIn普通动态已适配；枚举映射和自由文字显式区分。
- 发布：/api/linkedin/connection及start／callback；/api/publishing/previews、execute、records及reconcile。精确配置与状态见[接口说明](第二阶段执行与发布接口.md)。
- 执行目录 dataDir/tmp/generation；草稿与素材 dataDir/tmp/content。正式JSON备份不含这些内容。仅恢复正式备份而缺少原执行tmp时，局部Agent修改无法恢复原规则选项，须按提示重新生成，不能猜配置。

## 发布配置与后续验收条件

缺少实际LinkedIn应用配置、OIDC与Share产品权限、注册HTTPS回调，以及用户正式OAuth授权。配置在服务端安全完成，密钥不在聊天／页面输入、不进Git或运营备份；OAuth token仅内存，重启重新授权。HTTPS代理只转发callback路径，不能公开整个工作台。

预览绑定账号、授权会话、明确版本、正文／图片hash、恢复epoch和期限。远程调用前持久化submitting，只有真实201＋有效平台ID才published；网络未知、缺ID或重启中断均unknown，禁止自动重发。只读核对需已知ID及额外受限r_member_social；无ID时人工检查平台。

真实自动配图需要另一个已授权、官方或受支持的后台入口。当前会话imagegen可用不是后台接口，不提取会话token、不调用未公开端点。

完成端到端验收还需真实配图、真实OAuth权限、具体版本与账号的用户发布确认，以及平台返回ID／URL和图片实际显示。开发指令不能替代这次发布确认，也不得重发原文章作测试。

## 数据、启动与验证

Windows x64、Node.js≥22.13，当前环境22.23.2。启动和停止按[README](../README.md)，仅监听127.0.0.1。测试使用临时数据目录，浏览器隔离副本 tmp/stage2-browser、端口4331；真实data/未用于测试。收尾已用该目录Stop-Workbench.ps1停服并关闭测试标签，临时证据保留。停止对应目录的服务，不批量结束进程。

备份协议app content-workbench-local／version1不改；content恢复正式集合，publishing扩展合并保留发布事实和防重记录。旧备份保留缺失扩展的现有数据；恢复前快照与事务保留。成功恢复让旧tmp失效但可读，不自动删除、不发帖；活跃发布时拒绝恢复。旧程序不能读取新增扩展。

单素材4MiB、内容请求8MiB、完整备份16MiB；发布前预留结果元数据容量。日快照30份与恢复前快照20份沿用，不是新增tmp清理策略。Windows命名管道租约防同一dataDir双服务；Job Object负责受控模型进程的退出，不触及无关进程。

普通检查 npm ci、npm run build、npm test，源码／public变化后同步dist；真实模型和发布必须另列证据，不把测试替身当外部接通。本次按用户追加授权一并提交源码、文档、public和dist；后续commit／push仍须按当前任务授权执行。

## 保留的历史材料

首个产品是[system1-agents](https://github.com/ThinkFlowLab/system1-agents)，[作者原生长文章](https://www.linkedin.com/pulse/give-your-agents-jev-like-system-1-decision-model-kai-yuan-phd-ymhgc)只作参考，不重发。[第一阶段预检](第一轮接入预检.md)、[规则整理](第一轮文本规则整理.md)及[范围](第一轮实施范围.md)保留历史证据，cd81286已提交。

[用户参考索引](references/README.md)、[s1a-launch-kit](references/s1a-launch-kit/SKILL.md)和原DOCX保持原样。第一阶段试稿／配图位于tmp/round1-writing与tmp/round1-assets，不进Git或正式库；克隆不会带走临时证据。旧DOCX仅正文／OOXML检查，页面渲染仍未完成，本阶段未扩展DOCX。

本阶段完成后汇总并等待下一步授权；不自动扩展完整需求中尚未实现的部分。
