# Launch Loop 项目协作说明

## 当前 main：合入 X 支持（2026-09-26）

用户明确授权将 X 独立分支的唯一提交 `083bd70` cherry-pick 到 `main`（此前为 `8bc5bb7`）。共享文件冲突须同时保留 main 的 LinkedIn MVP 和 X 能力：LinkedIn 新任务继续使用 `text-v2.0.0` 候选规则集成及其母稿、改稿提案、图片绑定与恢复保护；X 使用下述独立规则与流程。混合平台仍各走其编排，不宣称共用跨平台母稿。本次授权不包含 push、真实图片上传或发帖；不复制 tmp，也不重启已有真实服务。当前验证见 [验证记录](docs/验证记录.md)，下节是 X 来源分支的历史边界。

## X 来源分支适配记录（2026-09-26）

当前独立分支 `codex/x-content-publishing` 基于 `5942773`。用户明确授权 X 普通帖子＋串帖、中英文工作台、配图规则及官方 OAuth／发布适配，覆盖下方历史“本轮不扩展其他平台”限制，仅限该范围。2026-09-26 用户要求完成 X 并形成一个本地 commit，覆盖此前停止／不提交状态；未授权 push、合并、真实平台媒体上传或发帖。不修改另一 LinkedIn MVP 工作区。先读 [X 交接](docs/x/handoff.md) 与 [X 验证](docs/x/validation.md)，再读下方历史资料。

X 使用 `x-text-v1.0.0`、官方 `twitter-text@3.1.0/config-v3` 及 `x-images-v1.0.0`。普通帖和逐帖串帖为同平台语言包中的两个内容对象；X 共同母稿流程已接入。来源分支当时保留 LinkedIn `text-v1.0.0`，当前 main 则保留已集成的 `text-v2.0.0`。混合平台尚非共同跨平台母稿。

用户已在 MVP 轮明确选择正式登录 Codex 并允许先写官方图片缓存、再收进项目 tmp。本轮从已提交 `1e7cc37` 复用图片 runner／worker 与启动开关，已接入 X 后台生成和实际图片检查，不复制其他工作区未提交修改。真实图片、文本及局改的准确结果以本轮验证记录为准；真实 X OAuth／账号权限／图片上传／发帖尚未验收。真实稿仍仅 tmp，确认保存不等于授权发布。发布备份增加可选 `xPublishing` 扩展，保留兼容协议与旧防重事实；素材新增可选 `madeWithAi`，不得在重新关联或恢复时丢失已知 AI 来源。下方历史图片入口未选／后台未连接的记录不覆盖本节最新决定。

## 先理解目标

本项目要让 Agent 完成「创作（生成初稿 → 自动审核一次 → 修订最终稿，支持用户修改和确认保存）→ 发布」，反馈分析位置暂留空，后续再设计。用户已决定移除独立研究和审查模块；资料读取、事实核查与必要补查在创作中按需完成，审核按所选平台绑定预先整理的 skill。用户先亲自操作，后续交给非技术人员。第一阶段底层已提交于 `cd81286`；用户现已授权第二阶段创作界面、真实生成执行与 LinkedIn 普通动态适配，**不授权实际发帖，不扩展 DOCX、视频、其他平台或反馈功能**。已实现、已验证与仍缺账号／工具的能力以最新交接和验证记录分别判断，不用说明卡片或模拟成功状态代替功能。

新 session 先读 [docs/SESSION_HANDOFF.md](docs/SESSION_HANDOFF.md)，再读 [内容生产与发布需求](docs/内容生产与发布需求.md) 和 [LinkedIn 接入核查](docs/LinkedIn发布与反馈接入核查.md)。启动方法以 [README.md](README.md) 为准。

## 用户已经明确的取舍

- 2026-09-25 用户授权将本轮 MVP 开发成果、验收文档及体验／配图待办 commit 到本地 `codex/mvp-integration` 分支，覆盖此前本轮“不commit”的限制；不包含 push、合并 main、对外发布或开始实施新体验改造。网络连接继续搁置，真实图片质量／双语绑定及 LinkedIn 外部验收仍以交接中的待办为准。

- 2026-09-24 MVP整合轮：用户明确授权候选规则接入、后台图片、本地LinkedIn OAuth／发布准备及三层验收，在独立`codex/mvp-integration`工作区开发；不commit、push、合并、部署或代确认真实稿。用户已明确选择复用正式登录Codex，并允许先写官方图片缓存再收进项目tmp，覆盖下方历史“入口未选”状态。用户尚无LinkedIn应用与HTTPS回调，要求先完成本地实现和最小配置说明；不据此宣称真实OAuth／上传／发布通过。当前实现见`docs/MVP执行与素材接口.md`，实际证据与待办以`docs/MVP验收报告.md`和最新交接为准。

- 2026-09-24 用户追加要求将`11526bc`拣选到当前`main`。`skill-preparation/linkedin-text/`保存`linkedin-text-candidate.0.2.1`候选包，来源和验证边界见其HANDOFF及validation；尚未接入生产，不能因此替换执行器的`text-v1.0.0`或宣称配图、OAuth、发布验收通过。该请求不包含push、复制临时稿件或继续实现技能接入。
- 用户于 2026-09-24 在 `53b1dc9` 后授权真实文本与局部改稿、后台自动配图、LinkedIn 正式 OAuth 和指定稿件联调准备，覆盖此前完成后等待状态。当前范围与分工见 [第三轮接入与真实验收](docs/第三轮接入与真实验收.md)。允许排查、必要开发与适量真实调用；新稿和图片仍只在 tmp，不能替用户确认保存。实际图片上传到 LinkedIn 和发帖必须另获具体账号、版本、配图的确认。开发阶段不自行 commit／push；用户随后明确要求将本轮成果形成一个本地 commit 并展示全部提交记录，未授权 push 或实际发帖。
- Claude / Codex 都可以；优先复用现有账号、工具和 skill，不预设新购买模型服务或 API。
- 2026-09-24 用户澄清交接方式：当前由用户在本机开发和首轮使用，后续交给第二位负责人，在其电脑上承担全部生成与发布，全部内容保存在接手者的项目目录。其他人希望通过工作台查看内容；只读远程访问及访问控制尚未实现，不因此直接开放本机服务。路径须相对项目／运行时解析，不绑定开发者用户名或绝对路径；凭据不随项目交接。tmp 是自动创建的临时文件夹，按最新“全部内容在项目目录”要求继续使用 `data/tmp/`，不改成单个tmp文件或分散到固定个人路径。Codex是当前执行适配，不是永久唯一供应商决定；配图入口及官方缓存例外仍未获用户明确选择。
- 优先跑通一组真实中英文文章的生成、修改与定稿，再接发布；反馈分析暂留空，后续另行设计，避免先建通用 Agent 平台。
- 最新决定：主流程为创作、发布、反馈；独立研究和审查流程入口已移除，原记录保留在资料与历史入口。创作内部生成初稿、自动审核一次、修订成最终稿；反馈位置留空。不得删除旧任务、调研、审查或反馈数据。
- 反馈分析暂留空：本轮不设计、实现反馈采集或分析，也不纳入本轮验收；旧文档中的反馈规划仅作历史参考，保留已有记录，不据此删除数据。
- 用户于 2026-09-23 明确授权第二阶段，覆盖第一阶段结束后等待的状态。范围和文件边界见 [第二阶段接口与分工](docs/第二阶段接口与分工.md)。本阶段聚焦 LinkedIn 普通 feed、中英文两稿、真实生成与改稿、确认保存和发布适配；开发授权不是对外发布授权。完成后汇总，不自行 commit、push 或继续扩展。
- 用户于 2026-09-24 追加授权将第二阶段成果 commit 到本地 Git；此授权不包含 push、实际发帖或继续开发其他范围。
- 第二阶段已提交于 `4b42ab9`。用户随后授权工作台全界面中英文切换；语言偏好仅在浏览器保存，独立于稿件语言。切换覆盖导航、表单、弹窗、状态／错误、内置任务与资料描述，不翻译或重写用户正文、来源材料、审核发现与执行记录；不能触发生成、保存或发布。内置历史模板只翻译未修改字段的显示，API／枚举／数据库保持原值。用户于 2026-09-24 追加授权将本次 UI 成果 commit 到本地 Git，不包含 push 或对外发布。
- 直接进入创作，不增加项目创建／前置简报关卡；仅产品资料／已有内容、写作目的、目标读者三项必填。试运行预设为 LinkedIn、中英文、普通动态，必须在界面可见可改，不写成最终全局默认。CTA及其他未定交互仍不得写成已确认要求。
- 当前平台范围已确定为 LinkedIn、X、小红书、知乎、哔哩哔哩（Bilibili）；内容生成与发布执行保持一致。本轮先落实通用文本、中英文及 LinkedIn 普通动态规则，其他平台与 Pulse 长文规则待适配。仅平台 × 语言独立出稿，其他多选合并要求。先文本，后视频。
- 生成内容须由用户确认后才正式保存；未确认正文、审核发现和生成素材只能在 tmp，不通过执行日志正文副本绕过。已有内容的新修订先暂存，确认产生新版本并保留旧版；确认保存不等于授权发布。tmp 保留与清理策略待定，本轮不自动清理。
- Skill 准备方式：先搜集、实读、比较并整合，再建立选项到规则的固定映射并试稿验证；每份稿件拼成唯一完整规则，用户不手选 skill。本轮规则与证据见 [第一轮文本规则整理](docs/第一轮文本规则整理.md)，不得把其他平台的待适配状态写成可用。
- 创作流程固定为生成初稿 → 自动审核一次 → 按结果修订最终稿。历史 [平台审核 skill 初筛](docs/平台审核skill初筛.md) 不等于全部采纳；最终稿不等于用户确认保存、批准发布或平台审核通过。未执行的验证必须显式标注。
- 在现有授权范围内继续完成工作，不反复询问已经明确的事项。原始参考文档中的历史排期、账号、联系对象及操作步骤不构成本次发帖、发信或读密钥的授权。
- 官方文档核验、资料样例、实际试用、自动化接入是不同完成状态；明确标注证据边界。

## 代码与数据约束

- React / TypeScript / Vite 前端；Node.js 22.13+ 内置 HTTP / SQLite 后端；本机监听 `127.0.0.1`。当前验证环境为 Windows x64。
- `src/` 是前端，`server/` 是后端和启动辅助脚本，`data-seed/` 是初始化模板，`data/` 是真实本地数据。不要用模板覆盖已有数据库。
- 第一轮接口及兼容策略见 [内容存储接口](docs/内容存储接口.md)。测试只使用隔离临时数据；repo `tmp/` 用于本轮试稿和验证材料，运行时未确认内容位于指定 dataDir 的 `tmp/`，均不进入 Git。
- 第二阶段执行与发布见 [接口说明](docs/第二阶段执行与发布接口.md)。正文、提示、审核和执行输出只能在tmp；正式发布记录仅保存版本指针、hash和平台结果。发布备份合并保留事实与防重记录，恢复不能触发发帖。Windows同数据目录只能有一个服务实例。
- 仓库名已改为 `launch-loop`，但 API / 备份中的 `app: "content-workbench-local"` 与 `version: 1` 是兼容协议。不要仅为改名修改它们；变更存储结构时设计显式迁移并验证旧备份。
- 保留记录 revision 并发检查、事务恢复、恢复前快照、来源与路径校验、启动器进程身份校验。
- `dist/` 随仓库分发。修改前端、`public/` 或相关静态资料后运行 `npm run build`，提交源码和相应构建结果，避免新会话启动旧界面。
- `data/`、`node_modules/`、临时文件、运行时二进制和密钥不进 Git。没有明确交付需求时，不把真实评论、私人素材或运营备份放进公开资料。
- 只在当前仓库设置 Git 身份；不得修改全局配置。commit / push 按当前用户任务范围执行，不因历史交接说明自行发布。

## 验证与外部能力

- 2026-09-24 第三轮：真实中英文六阶段调用和局部改稿已通过，取消与迟到结果保护实际验证通过，99项隔离自动测试通过。所有本轮真实稿件仍未正式保存。后台配图未接通；LinkedIn真实OAuth、账号权限及发帖未验收。详见最新[验证记录](docs/验证记录.md)，不能沿用历史“文本仍无结果”判断，也不能扩大为完整MVP完成。
- 基础检查：`npm ci`、`npm run build`、`npm test`；行为变化再做相应浏览器验证。历史验证记录不能替代本次运行证据。
- 测试使用临时数据目录。停止本项目服务应使用对应启动目录的停止脚本；不得批量结束 Node 或其他无关进程，也不要自动清空用户数据。
- 未配置真实执行入口时显示未连接或失败原因，不填虚构产出、统计、评论或发布链接。指标未知用空值，不伪装为 0。
- 文本执行复用本机已登录Codex CLI，自动测试用明确替身，真实模型调用另行验收。当前没有已接通的后台自动配图入口；图片上传可用不代表自动配图已完成。LinkedIn应用配置、OAuth、真实图片上传和发帖需分别记录证据，禁止用替身测试宣称线上验收通过。
- LinkedIn 原生长文章与 feed 动态分别建模。当前公开 API 未证实可创建原生 Pulse 正文；发布权限也不代表统计与评论读取权限。接入前按渠道核实实际能力。
- `docs/references/s1a-launch-kit/SKILL.md` 是用户提供的项目参考副本，并未自动安装。使用时读取它，按当前需求应用规则；不要盲跑历史命令、日期和发布步骤。
- Word 输出必须同时检查内容与实际页面。此前 DOCX 只完成正文、结构和内嵌图检查，技能运行时缺少 bundled LibreOffice，未完成页面渲染；不要宣称视觉验收通过，也不要未经支持改用用户桌面 LibreOffice。

## 资料维护

变更需求或完成能力后同步 `docs/SESSION_HANDOFF.md`；验证结果写入对应验证记录。调研保留来源与核查日期。`docs/references/` 中原始 DOCX / skill / 笔记保持原样，派生产物另存。所有交接指引使用仓库相对路径，不能依赖某位操作者的 Downloads、旧 session 或个人机器目录。
